tasks:
  - id: T-001
    nom: "Spec FS-06 Applications — backend"
    statut: done
    date_resolution: "2026-03-29"
    sprint: S1
    theme: "FS-06"
    type: spec
    feature: "FS-06-BACK"
    sessions:
      - tool: OC
        id: "a1b2c3"
        nom: "spec applications back"
    notes: "Spec NestJS complète : CRUD + RBAC + audit trail + pagination cursor"

  - id: T-002
    nom: "Choix dnd-kit vs MUI X pour drag-and-drop tags"
    statut: done
    date_resolution: "2026-04-05"
    sprint: S1
    theme: "F-03"
    type: decision
    feature: "F-03"
    sessions:
      - tool: OC
        id: "a3f9d1"
        nom: "dnd-kit decision"
      - tool: CL
        id: "b88c12"
        nom: "archi tags FS-21"
    notes: "Tranché → @dnd-kit (MIT), plus léger que MUI X Tree View pour ce besoin"

  - id: T-004
    nom: "Bug — Tri par nom non fonctionnel sur GET /applications"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: "docs/05-Project/20260408/SESSION-HANDOFF-back-t004-recette-fs07.md"
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-06"
    type: bug
    feature: "FS-06-BACK"
    sessions:
      - tool: CL
        id: "qa-ses"
        nom: "audit tests data-objects"
      - tool: OC
        id: "1dde61"
        nom: "fix sortBy=name test limit in Playwright"
    notes: |
      Détecté lors de la session QA Data Objects (2026-04-07).
      Test Playwright : tests/applications/applications-pagination.api.spec.ts:50.
      GET /applications?sortBy=name&sortOrder=asc — réponse 200 mais ordre non respecté.
      À corriger par agent back (ApplicationsService.findAll — clause orderBy).
      Résolution (2026-04-08) : le tri backend est correct, le test ne forçait pas la pagination.
      Root cause : `limit` implicite à 20, donc "Zebra App" pouvait être hors page 1.
      Fix : ajout de `limit=100` dans `e2e/tests/applications/applications-pagination.api.spec.ts`.
      Vérification : `playwright test --project=api-backend tests/applications/applications-pagination.api.spec.ts` → PASS (5) FAIL (0).

  - id: T-005
    nom: "Bug — Isolation données entre tests IT Components (DELETE 204/409)"
    statut: done
    assigned_agent: qa
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "QA"
    type: bug
    feature: "QA-IT-COMPONENTS"
    sessions:
      - tool: CL
        id: "qa-ses"
        nom: "audit tests data-objects"
      - tool: CL
        id: "enc-sq"
        nom: "fix cleanup Playwright — itComponents detach"
    notes: |
      Détecté lors de la session QA Data Objects (2026-04-07).
      Corrigé 2026-04-08 : test-data.fixture.ts createApplication cleanup —
      PATCH itComponents:[] avant DELETE (détache les ICs sans bloquer la suppression).
      Root cause : cleanup LIFO tentait DELETE app avant que les ICs soient nettoyés,
      hasDependencies=true → skip → app + domain orphelins.

  - id: T-010
    nom: "Implémenter FS-05-FRONT — Data Objects frontend"
    statut: done
    assigned_agent: front
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-05"
    type: impl
    feature: "FS-05-FRONT"
    sessions:
      - tool: CL
        id: "ark-osl"
        nom: "audit statut FS-05-FRONT — correction tracking"
    notes: |
      Spec FS-05-FRONT v0.1 stable — PNS-02 drawer, Chip isSourceOfTruth (success/default),
      Autocomplete type (freeSolo + database/dataset/file), colonne role (consumer/producer/owner).
      Gate : FS-05-BACK done ✅. Amendements A+B backend (isSourceOfTruth, type autocomplete) pré-requis.
      Implémentation confirmée 2026-04-08 : 10 fichiers frontend (pages, composants, API client,
      types, utils), 4 routes câblées dans App.tsx, toutes les US réalisées.

  - id: T-011
    nom: "Implémenter FS-07-BACK — Business Capabilities backend"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: "SESSION-HANDOFF.md"
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-07"
    type: impl
    feature: "FS-07-BACK"
    sessions:
      - tool: OC
        id: "d01cbc"
        nom: "impl FS-07-BACK business capabilities"
      - tool: OC
        id: "4babfd"
        nom: "Gate G-17 — openapi.yaml BC endpoints + schemas"
    notes: |
      Spec FS-07-Business-Capabilities-back.md v1.0 stable.
      Hiérarchie récursive WITH RECURSIVE, level auto-calculé, hiérarchie illimitée, prévention circulaire,
      N:N avec Applications, migration NFR-GOV-005.
      
      Implémentation complète (2026-04-08) :
      - Module NestJS (controller, service, DTOs, module, index)
      - 8 endpoints : CRUD + /tree + /:id/children + /:id/applications
      - Helpers : calculateLevel(), isDescendant(), recalculateLevelsRecursively()
      - 19 tests Jest unitaires
      - 20 tests Supertest e2e
      - 21 tests Playwright API (crud, validation, tree)
      - Seed 12 business capabilities (3 niveaux hiérarchiques)
      - Fix HttpExceptionFilter : propagation du champ 'details' dans les erreurs
      
      Gate G-17 validée (2026-04-08) : openapi.yaml mis à jour avec 8 paths + 5 schemas BC.
      Gate T-007 (tests domains-dependencies) débloquée.

  - id: T-013
    nom: "Fix cleanup Playwright — tests + global teardown"
    statut: done
    assigned_agent: qa
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "QA"
    type: impl
    feature: "QA-CLEANUP"
    sessions:
      - tool: CL
        id: "enc-sq"
        nom: "fix cleanup Playwright complet"
    notes: |
      Deux root causes corrigées :
      1. Applications créées via auth.request.post() directement (non traquées) →
         converties vers testData.createApplication() dans :
         - applications-crud.api.spec.ts (6 tests)
         - applications-validation.api.spec.ts (duplicate name test)
         - domains-crud.api.spec.ts (POST create domain)
      2. createApplication cleanup — PATCH itComponents:[] + DELETE inconditionnels
         (supprime le check hasDependencies qui bloquait le nettoyage).
      Ajout : e2e/global-teardown.ts + playwright.config.ts globalTeardown
      → détecte automatiquement les fuites post-run (log par préfixe de nom).

  - id: T-014
    nom: "Seed domaines + applications — affectation cohérente"
    statut: done
    assigned_agent: data
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "F-999"
    type: impl
    feature: "SEED"
    sessions:
      - tool: CL
        id: "enc-sq"
        nom: "seed domains + apps affectation cohérente"
    notes: |
      seed-applications.ts : ajout champ domainName par application,
      lookup par Map<name, domain> au lieu d'affectation aléatoire.
      10 domaines insérés via seed-domains.ts.
      25 applications insérées via seed-applications.ts.
      Répartition : Finance (3), IT (13), RH (1), Ventes (2),
      Marketing (1), Supply Chain (1), R&D (1), Service Client (1), Prod (0), Juridique (0).
      Purge préalable : 105 apps + 111 domaines + 84 IT components résidus tests supprimés via SQL.

  - id: T-015
    nom: "Tests UI Playwright — Business Capabilities frontend"
    statut: done
    assigned_agent: qa
    session_active: ~
    handoff_ref: "docs/05-Project/20260409/SESSION-HANDOFF-qa-t015-playwright-fs07.md"
    date_resolution: "2026-04-09"
    sprint: S3
    theme: "FS-07"
    type: test
    feature: "FS-07-FRONT"
    sessions:
      - tool: OC
        id: "7d2b4c"
        nom: "playwright ui fs-07 + stabilisation env local"
    notes: |
      Tests UI Playwright pour l'écran Business Capabilities (arbre hiérarchique, CRUD UI).
      Gate : FS-07-BACK done (T-011) ✅ + FS-07-FRONT implémenté (T-020) ✅.
      Fixture createBusinessCapability() déjà disponible dans e2e/fixtures/test-data.fixture.ts.
      Débloqué 2026-04-08 après T-020 done.

  - id: T-016
    nom: "Spec FS-07-FRONT — Business Capabilities frontend"
    statut: done
    assigned_agent: spec
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-07"
    type: spec
    feature: "FS-07-FRONT"
    sessions:
      - tool: CL
        id: "6b4a8f"
        nom: "rédaction spec FS-07-FRONT v1.0"
    notes: |
      Spec frontend Business Capabilities : 3 vues (liste arborescente, arbre MUI TreeView, matrix),
      drawer read-only, formulaire avec criticality/technicalFit, agrégation récursive client-side.
      US01-05, US06/07 révisées, US09, US10, US13, US16, US19.
      Gate : T-019 amendment backend (criticality + technicalFit) done avant impl T-018.
      
      Spec v1.0 rédigée (1000+ lignes) : 13 sections, 8 composants, 7 règles métier frontend.
      Amendment T-019 documenté dans FS-07-BACK v1.1 (Prisma + OpenAPI + migration SQL).
      Décisions architecture : D-01 (enums scalaires), D-02 (MUI TreeView), D-03 (agrégation client-side),
      D-04 (US06 reformulée), D-05 (AppBreadcrumbs PNS-11).

  - id: T-018
    nom: "Implémenter FS-07-FRONT phase 1 — Fondation + chips"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: "docs/05-Project/20260408/SESSION-HANDOFF-front-fs07-phase1.md"
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-07"
    type: impl
    feature: "FS-07-FRONT"
    sessions:
      - tool: OC
        id: "e4a7f2"
        nom: "fondation FS-07-FRONT (types, API, utils, chips)"
    notes: |
      Phase 1 complète : fondation technique + 3 composants chips.
      
      Fichiers créés :
      - Types : businessCapability.ts (CriticalityLevel, TechnicalFitLevel, BusinessCapability, TreeNode, FormValues, ApplicationMapping, ViewMode)
      - API : businessCapabilities.ts (8 hooks React Query)
      - Utils : businessCapability.utils.ts (sumApplications, isDescendant, buildHierarchyPath, flattenTree, getRootNodeIds)
      - Composants : CriticalityChip, TechnicalFitChip, AppLifecycleBreakdown
      - AppBreadcrumbs : déjà existant, conforme PNS-11
      
      Reste à faire (T-020) :
      - 5 composants : Form, Drawer, Tree, Matrix + installer @mui/x-tree-view
      - 4 pages : List, New, Detail, Edit
      - Clés i18n section businessCapabilities
      - Tests manuels checklist §12
      
      Gates : T-016 spec stable + T-019 amendment backend done.

  - id: T-019
    nom: "Amendment FS-07-BACK — champs criticality + technicalFit"
    statut: done
    assigned_agent: back
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-07"
    type: impl
    feature: "FS-07-BACK"
    sessions:
      - tool: OC
        id: b87ec4
        nom: "amendment backend criticality + technicalFit"
    notes: |
      Ajout 2 champs enum sur BusinessCapability : criticality (LOW/MEDIUM/HIGH/CRITICAL),
      technicalFit (ADEQUATE/PARTIAL/INADEQUATE/LEGACY).
      Migration SQL (CREATE TYPE + ALTER TABLE), Prisma model, DTOs (Create/Update/Response/ListItem/TreeNode),
      seed (12 capabilities alimentées avec valeurs réalistes), tests manuels validés.
      Gate T-018 (impl FS-07-FRONT) débloquée.

  - id: T-020
    nom: "Implémenter FS-07-FRONT phase 2 — Composants + pages + i18n"
    statut: done
    assigned_agent: front
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-07"
    type: impl
    feature: "FS-07-FRONT"
    sessions:
      - tool: OC
        id: "5ecde5"
        nom: "impl FS-07-FRONT phase 2 complète"
    notes: |
      Phase 2 complète : 5 composants + 4 pages + i18n + routes.
      
      Composants créés (5) :
      1. BusinessCapabilityForm (validation client-side, exclusion descendants via isDescendant, Select criticality/technicalFit)
      2. BusinessCapabilityDrawer (PNS-02 read-only, breadcrumb hiérarchique via buildHierarchyPath, tabs Info/Applications, AppLifecycleBreakdown)
      3. BusinessCapabilityTree (MUI x-tree-view SimpleTreeView + TreeItem, expand/collapse natif, CriticalityChip inline)
      4. BusinessCapabilityMatrix (tuiles imbriquées Grid + Card, coloration getCriticalityColor, sumApplications recursive, Tooltip breakdown)
      5. getCriticalityColor util ajouté dans businessCapability.utils.ts
      
      Pages créées (4) :
      1. BusinessCapabilitiesPage (liste arborescente flat + vue tree + vue matrix, ToggleButtonGroup, filtres search+domain, drawer)
      2. BusinessCapabilityNewPage (AppBreadcrumbs PNS-11, BusinessCapabilityForm, tagsApi.getDimensions)
      3. BusinessCapabilityDetailPage (read-only, CriticalityChip, TechnicalFitChip, TagChipList, navigate on parent click)
      4. BusinessCapabilityEditPage (3 tabs Général/Relations/Audit, reparenting inline, mini-table applications, EmptyState audit P2)
      
      i18n : section businessCapabilities (~100 clés) ajoutée dans fr.json
      
      Routes : câblées dans App.tsx (/business-capabilities, /new, /:id, /:id/edit)
      
      Extension PageHeader : ajout prop secondaryAction pour le ToggleButtonGroup
      
      Dépendance : @mui/x-tree-view installé
      
      TypeScript : 0 erreurs spécifiques aux fichiers FS-07-FRONT (erreurs préexistantes dans IT-Components/Applications non liées)

  - id: T-021
    nom: "Fix YAML indentation openapi.yaml — dette technique"
    statut: done
    assigned_agent: arch
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "F-999"
    type: debt
    feature: "OPENAPI"
    sessions:
      - tool: OC
        id: "4babfd"
        nom: "fix 457 lignes indentation impaire → paire"
    notes: |
      openapi.yaml avait 457 lignes avec indentation impaire (1, 3, 5, 7+ espaces)
      au lieu de l'indentation standard 2-espaces YAML.
      Python yaml.safe_load() rejetait le fichier entier.
      
      Correction : normalisation de toutes les lignes à indentation paire.
      Paths affectés : /providers/{id}/applications, /applications, /applications/{id},
      /applications/{id}/dependencies, /applications/{id}/it-components, /it-components.
      
      Commit : 4babfd7 (inclus avec Gate G-17 FS-07-BACK).

  - id: T-022
    nom: "Fix erreurs TypeScript préexistantes — IT Components + Applications"
    statut: done
    assigned_agent: front
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "F-999"
    type: debt
    feature: "TS-FIX"
    sessions:
      - tool: OC
        id: "5ecde5"
        nom: "fix 5 erreurs TS ITComponent + Application"
      - tool: OC
        id: "c3f8a1"
        nom: "stabilisation FS-07-FRONT bugfix session"
    notes: |
      5 erreurs TypeScript préexistantes corrigées (bloquaient npm run build) :
      1. ITComponentDrawer.tsx:56 — EntityTagResponse[] → TagValueResponse[] mapping inline
      2. ITComponentListPage.tsx:176 — EntityTagResponse[] → TagValueResponse[] mapping inline
      3. ApplicationNewPage.tsx:135 — PaginatedResponse<Domain> → SelectOption[] (domainOptions)
      4. ApplicationEditPage.tsx:158 — PaginatedResponse<Domain> → SelectOption[] (domainOptions)
      5. ITComponentFormPage.tsx:128 — technology: null → undefined (mapping explicite)
      
      Détectées lors de T-020 (FS-07-FRONT phase 2).
      Root cause : types API (EntityTagResponse, PaginatedResponse) vs types formulaires (TagValueResponse, SelectOption).

  - id: T-023
    nom: "Fix API businessCapabilities/tree — response.data.data extraction"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: "docs/05-Project/20260408/SESSION-HANDOFF-front-fs07-bugfix.md"
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-07"
    type: bug
    feature: "FS-07-FRONT"
    sessions:
      - tool: OC
        id: "c3f8a1"
        nom: "fix useBusinessCapabilitiesTree response extraction"
    notes: |
      Bug : nodes.reduce is not a function sur la page Business Capabilities.
      Root cause : GET /business-capabilities/tree retourne { data: [...] }
      mais useBusinessCapabilitiesTree retournait response.data (= { data: [...] })
      au lieu de response.data.data (= [...]).
      
      Fix : frontend/src/api/businessCapabilities.ts ligne 56-59
      - Avant : client.get<BusinessCapabilityTreeNode[]>(...) + return response.data
      - Après : client.get<{ data: BusinessCapabilityTreeNode[] }>(...) + return response.data.data
      
      Également résolu cette session :
      - Installation @mui/x-tree-view dans container Docker (npm install)
      - Restart container frontend
      - Fix 5 erreurs TypeScript préexistantes (T-022)

  - id: T-024
    nom: "Recette FS-07-FRONT — Business Capabilities frontend"
    statut: done
    assigned_agent: qa
    session_active: ~
    handoff_ref: "docs/05-Project/20260408/SESSION-HANDOFF-qa-t024-recette-fs07.md"
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-07"
    type: review
    feature: "FS-07-FRONT"
    sessions:
      - tool: OC
        id: "f8a3d1"
        nom: "recette statique fs-07 + hotfix conformité"
    notes: |
      Phase de recette de l'implémentation frontend Business Capabilities (T-018, T-020).
      Valider la checklist §12 de la spec FS-07-BACK (synthèse : 3 vues, PNS-11, toggle/view switching,
      expand/collapse, drawer PNS-02, CriticalityChip + TechnicalFitChip, agrégation récursive,
      erreurs 400 CIRCULAR_REFERENCE / 409 DEPENDENCY_CONFLICT).

      Gates considérées : FS-07-FRONT implémentée (T-020) + APIs FS-07-BACK et amendements (T-019).
      Lié à la livraison : T-024.

  - id: T-025
    nom: "Amendment FS-06-BACK — capabilityIds dans DTOs + sync app_capability_map"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: "docs/05-Project/20260408/SESSION-HANDOFF-back-t025-fs06-bc.md"
    date_resolution: 2026-04-08
    sprint: S3
    theme: FS-07
    type: impl
    feature: FS-06-BACK
    sessions:
      - tool: OC
        id: "b4ca76"
        nom: "impl capabilityIds + validation build/e2e 21/21 + commit"
    notes: |
      Amendment FS-06-BACK pour implémenter la liaison Application ↔ BusinessCapability.
      La table app_capability_map et la relation Prisma existent déjà.
      À faire :
      - capabilityIds?: string[] dans CreateApplicationDto + UpdateApplicationDto
      - validateForeignKeys() : vérifier les IDs capabilities (pattern itComponents)
      - findOne() + findAll() : inclure capabilities dans Prisma include
      - mapApplicationResponse() : mapper businessCapabilities dans la réponse
      - create() + update() : sync junction app_capability_map (delete+recreate, pattern itComponents)
      ✅ Code + OpenAPI + tests Supertest/Playwright complétés pour le support BC.
      ✅ Validation exécutée dans le container backend : `npm run build` OK + `npm run test:e2e -- --runInBand test/FS-06-applications.e2e-spec.ts` OK (21/21).
      Gate T-026 (front) : ce back amendment doit être done avant.

  - id: T-026
    nom: "Amendment FS-06-FRONT — Sélecteur Business Capabilities dans Applications"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-10"
    sprint: S3
    theme: FS-07
    type: impl
    feature: FS-06-FRONT
    sessions:
      - tool: CL
        id: "c4d8e2"
        nom: "impl capabilityIds selector FS-06-FRONT"
    notes: |
      Amendment FS-06-FRONT : liaison Application ↔ BusinessCapability exposée côté UI.

      Livraison (2026-04-10) :
      - types/application.ts : BusinessCapabilityMapping, businessCapabilities[] sur Application/ListItem, capabilityIds dans ApplicationFormValues
      - ApplicationForm.tsx : section BC avec Dialog multi-select (pattern itComponents, sans rôle), prop availableOptions.businessCapabilities
      - ApplicationNewPage + ApplicationEditPage : useBusinessCapabilities({ limit: 200 }), capabilityIds initial/init depuis BC existantes
      - ApplicationDetailPage : chips cliquables vers /business-capabilities/:id dans section Relations
      - fr.json : 7 clés applications.form.businessCapabilitiesLabel + relations.businessCapabilities*

      Build frontend : 0 erreurs TypeScript, 0 erreurs Vite.
      Note : BusinessCapabilityEditPage.tsx:296 string hardcodée non adressée (hors scope T-026).

  - id: T-027
    nom: "Commande make db-reset-reseed + seed-all.ts orchestrateur"
    statut: done
    assigned_agent: data
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "F-999"
    type: impl
    feature: "SEED"
    sessions:
      - tool: CL
        id: "f3a912"
        nom: "make db-reset-reseed + seed-all.ts + fix app_provider_map"
    notes: |
      Makefile : cible db-reset-reseed — TRUNCATE CASCADE toutes tables métier
      (entity_tags, app_capability_map, app_data_object_map, app_it_component_map,
      app_provider_map, interfaces, applications, business_capabilities, data_objects,
      it_components, providers, domains, tag_values, tag_dimensions, audit_trail)
      puis relance seed-all.ts.
      Tables préservées : users, roles, permissions, role_permissions, refresh_tokens.
      backend/prisma/seed-all.ts : orchestrateur 6 seeds dans l'ordre.
      backend/package.json : prisma.seed → seed-all.ts (était seed.ts seul).
      Vérification : 10 domains + 25 applications + token OK.

  - id: T-028
    nom: "Fix GET /domains — réponse non paginée (page /domains vide)"
    statut: done
    assigned_agent: back
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "F-999"
    type: bug
    feature: "DOMAINS"
    sessions:
      - tool: CL
        id: "f3a912"
        nom: "fix domains.service.ts findAll pagination"
    notes: |
      Page /domains vide malgré 10 domaines en base.
      Root cause : DomainsService.findAll() retournait Domain[] brut,
      le frontend attend { data: Domain[], meta: { page, limit, total, totalPages } }.
      Fix : QueryDomainsDto créé, findAll() mis à jour avec count+skip+take+meta,
      controller @Query() ajouté.
      Fichiers : dto/query-domains.dto.ts (créé), domains.service.ts, domains.controller.ts.

  - id: T-029
    nom: "Uniformiser le menu d'actions des listes frontend"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: "docs/05-Project/20260408/SESSION-HANDOFF-front-list-actions-menu.md"
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-05"
    type: impl
    feature: "FS-05-FRONT"
    sessions:
      - tool: OC
        id: "2b0571"
        nom: "uniformize list row actions menu and add shared component"
    notes: |
      Refactor des listes Applications, Business Capabilities, Composants IT, Fournisseurs,
      Domaines et Data Objects pour remplacer les icônes d'action inline (Edit/Delete)
      par un menu à 3 points réutilisable.

      Actions du menu : Voir détails, Modifier, Supprimer.
      Point de sortie commun : création du composant partagé `RowActionsMenu`.

      Fichiers modifiés :
      - frontend/src/components/shared/RowActionsMenu.tsx
      - frontend/src/components/shared/index.ts
      - frontend/src/i18n/locales/fr.json
      - frontend/src/pages/applications/ApplicationsListPage.tsx
      - frontend/src/pages/business-capabilities/BusinessCapabilitiesPage.tsx
      - frontend/src/pages/it-components/ITComponentListPage.tsx
      - frontend/src/pages/providers/ProvidersListPage.tsx
      - frontend/src/pages/domains/DomainsListPage.tsx
      - frontend/src/pages/data-objects/DataObjectListPage.tsx

      Validation : `npm run build` (frontend) validé.
      Commit de clôture : 2b0571b.

  - id: T-030
    nom: "Recette FS-07-FRONT — correctifs conformité checklist §12"
    statut: done
    assigned_agent: front
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-07"
    type: bug
    feature: "FS-07-FRONT"
    sessions:
      - tool: OC
        id: "f8a3d1"
        nom: "recette QA + hotfix conformité FS-07"
    notes: |
      Défauts détectés pendant T-024 (recette checklist §12) et corrigés immédiatement :
      - Expand/collapse liste arborescente : enfants non masqués (filtre hiérarchique inactif)
      - Tri criticality : valeurs nulles placées en premier (au lieu de nulls last)
      - Strings en dur dans composants/pages FS-07 (i18n non conforme)

      Correctifs appliqués :
      - flattenTree() enrichi avec parentId/parent pour supporter le filtrage hiérarchique
      - BusinessCapabilitiesPage : masquage réel des descendants repliés + tri criticality nulls-last
      - Remplacement des textes hardcodés par clés i18n dans Drawer/Matrix/Detail/Edit/List
      - fr.json : ajout des clés businessCapabilities.common.*, matrix.*, drawer.close/moreApplications,
        detail.noDomain/rootCapability/subtitle, form.relatedApplicationsCount/applicationsRelationHint

      Vérification : `rtk npm run build` (frontend) OK.

  - id: T-031
    nom: "Bug — Onglet Applications vide sur détail IT Component (liaisons non affichées)"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-09"
    sprint: S3
    theme: "FS-05"
    type: bug
    feature: "QA-IT-COMPONENTS"
    sessions:
      - tool: CL
        id: "f26390"
        nom: "investigation + fix backend + fixes frontend Drawer/DetailPage"
    notes: |
      Root cause : GET /it-components/:id/applications reçoit page/limit comme strings via @Query()
      (sans DTO NestJS), mais Prisma exige des integers pour skip/take.
      Fix : backend/src/it-components/it-components.service.ts — Number(query.page) || 1 / Number(query.limit) || 20.
      Frontend : ajout CircularProgress + reset appsPage dans Drawer (useEffect on itComponent?.id).

  - id: T-033
    nom: "Business capabilities FIX : retirer la vue tree"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-11"
    sprint: S3
    theme: "FS-07"
    type: debt
    feature: "FS-07-FRONT"
    sessions:
      - tool: CL
        id: "buzzing"
        nom: "suppression BusinessCapabilityTree + toggle 2 vues + tests Playwright"
    notes: |
      Retirer la vue tree des Business Capabilities.
      Évaluer d'abord : specs (FS-07-FRONT) + composants front (BusinessCapabilityTree) + couverture de tests.
      Résolution 2026-04-11 : BusinessCapabilityTree.tsx supprimé, import AccountTreeIcon retiré,
      ToggleButton tree retiré, bloc render tree retiré, ViewMode réduit à 'list'|'matrix',
      clé i18n views.tree supprimée, tests Playwright mis à jour (2 vues, bascule liste/matrix).

  - id: T-034
    nom: "Business capabilities EVOL : vue matrix — chip criticité"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-11"
    sprint: S3
    theme: "FS-07"
    type: impl
    feature: "FS-07-FRONT"
    sessions:
      - tool: CL
        id: "buzzing"
        nom: "BusinessCapabilityMatrix — fond neutre + CriticalityChip + suppression badge niveau"
    notes: |
      Remplacer la couleur de background par un CriticalityChip dans la vue matrix.
      Supprimer la mention de niveau L0/L1/L2, afficher la criticité à la place.
      Objectif : alléger l'affichage visuel trop chargé avec les couleurs de fond actuelles.
      Résolution 2026-04-11 : getCriticalityColor + useTheme retirés, backgroundColor supprimé,
      badge L{level} supprimé, CriticalityChip ajouté dans le corps de la tuile.

  - id: T-035
    nom: "Business capabilities EVOL : vue matrix — limiter à 2 domaines L0 en desktop wide"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-11"
    sprint: S3
    theme: "FS-07"
    type: impl
    feature: "FS-07-FRONT"
    sessions:
      - tool: CL
        id: "buzzing"
        nom: "BusinessCapabilityMatrix — md={6} unifié (2 colonnes L0 en desktop)"
    notes: |
      En format desktop wide, limiter l'affichage à 2 domaines L0 maximum par rangée (actuellement 3).
      Améliore la lisibilité sur écran large.
      Résolution 2026-04-11 : md={depth===0?4:6} → md={6} unifié dans renderTile.

  - id: T-036
    nom: "Polishing UI — Uniformisation cadre blanc pages détail (BusinessCapability, DataObject, ITComponent)"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-11"
    sprint: S3
    theme: "POLISH"
    type: impl
    feature: ~
    sessions:
      - tool: CL
        id: "a3f7e1"
        nom: "Paper elevation=0 + cadre blanc sur 3 pages détail + AppBreadcrumbs/PageHeader ITComponent"
    notes: |
      Aligner BusinessCapabilityDetailPage, DataObjectDetailPage et ITComponentDetailPage
      sur le rendu "cadre blanc" de ApplicationDetailPage
      (référence visuelle : /applications/11a1cd07-6eaf-4951-9847-15ef884ef35d).
      Checklist :
      - [x] BusinessCapabilityDetailPage — Stack → Paper elevation=0 border divider, bouton retour sorti du Paper
      - [x] DataObjectDetailPage — Tabs+contenu → Paper elevation=0 border divider, px=2 sur tabs, p=4 sur info
      - [x] ITComponentDetailPage — Breadcrumbs inline → AppBreadcrumbs, titre Box → PageHeader, Tabs+contenu → Paper elevation=0
      Build frontend : 0 erreurs TypeScript, 0 erreurs Vite.

  - id: T-037
    nom: "Spec FS-07-FRONT v1.1 — cohérence post T-033/T-034/T-035"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-11"
    sprint: S3
    theme: "FS-07"
    type: doc
    feature: "FS-07-FRONT"
    sessions:
      - tool: CL
        id: "buzzing"
        nom: "mise à jour spec v1.1 : suppression vue tree, matrix chip, 2 vues"
    notes: |
      Mise en cohérence de docs/03-Features-Spec/FS-07-Business-Capabilities-front.md
      après implémentation T-033/T-034/T-035.
      Corrections : titre 3→2 vues, §4.2 marquée supprimée, §4.3 matrix CriticalityChip,
      §5 fichiers BusinessCapabilityTree retiré, §6 i18n views.tree retiré,
      §9 gate MUI TreeView retirée, §10/§12 tests mis à jour, §11 commande OpenCode nettoyée.

  - id: T-038
    nom: "Business Capabilities : Vue liste — Évolution UX du tri + indicateurs visuels de profondeur"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-12"
    sprint: S3
    theme: "FS-07"
    type: impl
    feature: "FS-07-FRONT"
    sessions:
      - tool: CL
        id: "f9d3b1"
        nom: "tri hiérarchique + visuels depth + expand/collapse all"
    notes: |
      Le pattern UX standard (entêtes de colonnes cliquables ASC/DESC) s'applique mal
      à la liste arborescente des Business Capabilities : les nœuds enfants "suivent"
      leur parent, ce qui brise l'ordre global attendu par l'utilisateur.

      Résolution 2026-04-12 :
      - sortTreeHierarchically() dans businessCapability.utils.ts : tri récursif par siblings
      - TableSortLabel supprimés ; Select "Trier par" (Nom A→Z/Z→A, Criticité ↑/↓) dans toolbar
      - Indicateurs visuels de profondeur : gras sur L0, FiberManualRecord (●) sur feuilles,
        chips niveau L0/L1/L2/L3 en dégradé gris, couleur texte dégradée (#424242→#BDBDBD)
      - underline="hover" sur les liens de noms
      - Bouton expand/collapse all (IconButton + Tooltip, icône UnfoldMore/UnfoldLess) dans toolbar
      Bug résiduel → T-041 (recherche libre ne fonctionne plus).

  - id: T-040
    nom: "Implémentation FS-08-BACK — Module Interfaces (NestJS + Prisma + tests)"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: "docs/05-Project/20260412/SESSION-HANDOFF-back-t040-fs08.md"
    date_resolution: "2026-04-12"
    sprint: S4
    theme: "FS-08"
    type: impl
    feature: "FS-08-BACK"
    sessions:
      - tool: CL
        id: "fs08sp"
        nom: "rédaction spec FS-08-BACK v1.1 — PRD Interfaces backend"
      - tool: OC
        id: "a9f3e1"
        nom: "impl FS-08-BACK module NestJS + tests"
    notes: |
      Spec stabilisée : docs/03-Features-Spec/FS-08-Interfaces-back.md (v1.1)
      Débloque : T-006 (tests Playwright applications-dependencies), T-039 (amendment FS-06-BACK)

      Périmètre :
      - Migration schéma (enums InterfaceType/InterfaceFrequency, fix id/updatedAt,
        drop tags[], drop latency_ms, technical_contact texte libre, add description/comment)
      - Module NestJS : interfaces.module/controller/service + DTOs create/update/query
      - Tests Jest unit (13 cas) + Supertest e2e (18 cas)
      - Seed 2 interfaces de démo (dépend des apps seedées FS-06)
      - openapi.yaml mis à jour (NFR-GOV-001)

  - id: T-041
    nom: "Bug — BC vue liste : la recherche libre ne fonctionne plus"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-12"
    sprint: S3
    theme: "FS-07"
    type: bug
    feature: "FS-07-FRONT"
    sessions:
      - tool: CL
        id: "f4a8c2"
        nom: "fix getVisibleRows — bypass expand filter quand search/domainFilter actif"
    notes: |
      Signalé après les évolutions T-038 (tri hiérarchique) et améliorations visuelles.
      La recherche dans la barre de filtres de la vue liste BC semble ne plus déclencher
      de filtrage. À investiguer : vérifier le flux searchInput → searchValue (handleSearchSubmit),
      l'impact du useMemo flatList sur la dépendance treeData, et si le useEffect
      d'initialisation de expanded interfère avec les résultats filtrés.

      Fix : dans getVisibleRows(), ajout de `if (searchValue || domainFilter) return flatList;`
      avant la logique expand/collapse. Quand un filtre est actif, le backend retourne déjà
      les résultats filtrés — l'expand state n'a pas à masquer ces résultats.

  - id: T-042
    nom: "Amendment FS-08-BACK — champ middlewareAppId (composant de médiation)"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-12"
    sprint: S4
    theme: "FS-08"
    type: impl
    feature: "FS-08-BACK"
    sessions:
      - tool: OC
        id: "c7d3e9"
        nom: "amendment backend middlewareAppId"
    notes: |
      ✅ Amendment FS-08-BACK complété — champ middlewareAppId (composant de médiation optionnel).
      
      Livraison 2026-04-12 :
      - Prisma : middlewareAppId + relation InterfaceMiddlewareApp + index idx_interfaces_middleware
      - Model Application : middlewareInterfaces[] relation inverse
      - Migration : ALTER TABLE interfaces ADD COLUMN middleware_app_id UUID
      - DTOs : middlewareAppId dans CreateInterfaceDto + UpdateInterfaceDto (PartialType) + QueryInterfaceDto
      - Service : validateApplications() étendue avec middleware, include middlewareApp dans findAll/findOne/create/update
      - Service update() : support unset explicite middlewareAppId=null
      - openapi.yaml : query param middlewareAppId + champs middlewareAppId/middlewareApp dans InterfaceResponse + InterfaceListItem + Create/Update DTOs
      - Tests unitaires : 20/20 pass (15 existants + 5 nouveaux : filtre middlewareAppId, create avec middleware, update set/unset, 404 middleware)
      - Tests e2e : 25 cas définis (bloqués par contexte auth en test — validé manuellement)
      - Seed : Interface "Portail → API Gateway" avec middleware (apps[3]) + SQL middleware_app_id
      
      Validation manuelle OK :
      - GET /api/v1/interfaces retourne middlewareApp + middlewareAppId
      - GET /api/v1/interfaces?middlewareAppId=<uuid> filtre correctement (1 résultat)
      
      Gate T-043 (FS-08-FRONT) débloquée — prêt pour implémentation frontend.

  - id: T-043
    nom: "Spec FS-08-FRONT — Interfaces frontend"
    statut: done
    assigned_agent: spec
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-12"
    sprint: S4
    theme: "FS-08"
    type: spec
    feature: "FS-08-FRONT"
    sessions:
      - tool: OC
        id: "curr-ses"
        nom: "rédaction spec FS-08-FRONT v0.1"
    notes: |
      Spec frontend Interfaces v0.1 rédigée (docs/03-Features-Spec/FS-08-Interfaces-front.md).
      Pattern PNS-02 drawer read-only, 4 pages (List/Detail/New/Edit), filtres serveur.
      Colonnes liste : Source | Middleware | Cible | Type (StatusChip FR) | Criticité (CriticalityChip) | Fréquence.
      Filtres : sourceApp + middlewareApp + targetApp + type + criticality.
      RM-IF-01 : validation client-side sourceApp ≠ targetApp avant soumission.
      Suppression libre (pas de DEPENDENCY_CONFLICT).
      Enums avec labels FR via i18n (interfaces.type.*, interfaces.frequency.*).
      Dépend de T-042 (amendment middlewareAppId) pour passer à stable.
      FS-08-Interfaces-back.md mis à jour statut → done.

  - id: T-044
    nom: "Implémentation FS-08-FRONT — Module Interfaces (React + MUI)"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-12"
    sprint: S4
    theme: FS-08
    type: impl
    feature: FS-08-FRONT
    sessions:
      - tool: OC
        id: "a3f8e1"
        nom: "impl FS-08-FRONT module Interfaces"
    notes: |
      Toutes les gates de la spec FS-08-Interfaces-front.md §9 sont levées :
      - T-040 (FS-08-BACK) ✅ done 2026-04-12
      - T-042 (amendment middlewareAppId) ✅ done 2026-04-12
      - T-043 (spec FS-08-FRONT) ✅ done 2026-04-12

      Prérequis manuels avant session OpenCode :
      1. Ajouter clés interfaces.* dans src/i18n/locales/fr.json (cf. spec §6)
      2. Câbler routes dans App.tsx (cf. spec §8)
      3. Ajouter entrée Sidebar Interfaces dans AppShell (CompareArrowsIcon)
      4. Passer FS-08-Interfaces-front.md statut → stable

      Périmètre :
      - InterfaceListPage + InterfaceDrawer (PNS-02) + filtres serveur
      - InterfaceDetailPage, InterfaceNewPage, InterfaceEditPage
      - InterfaceForm avec RM-IF-01 (sourceApp ≠ targetApp)
      - Hooks React Query (useInterfaces, useInterface, CRUD mutations)
      - types/interface.ts + utils/interfaces.utils.ts (formatInterfaceTitle)
      - CriticalityChip réutilisé depuis @/components/business-capabilities/

      Tests Playwright UI délégués à l'agent qa (cf. spec §10).

  - id: T-045
    nom: "Bug — Erreur 500 sur PATCH /interfaces/:id lors de l'enregistrement"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-12"
    sprint: S4
    theme: FS-08
    type: bug
    feature: FS-08-BACK
    sessions:
      - tool: OC
        id: "1dde62"
        nom: "fix setCurrentUser interfaces - use $executeRawUnsafe"
    notes: |
      Root cause : interfaces.service.ts utilisait sa propre méthode setCurrentUser() avec $executeRaw
      (paramétré), ce qui générait SET LOCAL ark.current_user_id = $1 — invalide pour PostgreSQL.
      Tous les autres services utilisent this.prisma.setCurrentUser() qui emploie $executeRawUnsafe.
      Fix : suppression de la méthode privée setCurrentUser() dans interfaces.service.ts,
      remplacement des 2 appels (create + update) par this.prisma.setCurrentUser(userId).
      Vérification frontend : PATCH /api/v1/interfaces/:id fonctionne correctement.

  - id: T-046
    nom: "IT Components : Vue liste — Changement affichage nombre apps en Chip MUI"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-12"
    sprint: S3
    theme: "FS-05"
    type: impl
    feature: "FS-05-FRONT"
    sessions:
      - tool: OC
        id: "k2-5front"
        nom: "uniformisation Chips compteurs vues liste (IT/Providers/DataObjects)"
    notes: |
      Uniformisation des compteurs d'applications liées en Chip MUI outlined+info.
      (Anciennement T-037 doublon — renommé T-046 lors de la consolidation des archives)
      
      Fichiers modifiés :
      - ITComponentListPage.tsx : Chip outlined+info, conditionnel si > 0
      - ProvidersListPage.tsx : idem pour appProviderMaps
      - DataObjectListPage.tsx : idem pour appDataObjectMaps
      
      Pattern établi : Chip size=small variant=outlined color=info,
      cellule vide si valeur = 0 pour alléger l'affichage.
      Commit : 7d72a00

  - id: T-006
    nom: "Tests applications-dependencies bloqués — FS-08 Interfaces non implémenté"
    statut: done
    assigned_agent: qa
    session_active: ~
    date_resolution: "2026-04-12"
    sprint: S4
    theme: "FS-08"
    type: bug
    feature: "FS-08-BACK"
    sessions:
      - tool: CL
        id: "qa-ses"
        nom: "audit tests data-objects"
      - tool: OC
        id: "a9f3e1"
        nom: "déblocage T-006 après T-040 FS-08-BACK done"
      - tool: OC
        id: "a4f2e1"
        nom: "correction tests + validation 4/4 pass (2026-04-12)"
    notes: |
      Détecté lors de la session QA Data Objects (2026-04-07).
      4 tests Playwright en échec dans tests/applications/applications-dependencies.api.spec.ts :
        - DELETE 409 when application has interfaces linked
        - DELETE 409 with detailed counts
        - GET /dependencies hasDependencies=true when linked
        - GET /dependencies hasDependencies=false for isolated app
      ✅ Gate levée : FS-08-BACK done (T-040, 2026-04-12).
      ✅ Gate levée : T-039 done (2026-04-12) — getDependencies() compte sourceInterfaces + targetInterfaces,
         remove() lève DEPENDENCY_CONFLICT. Tests e2e Supertest FS-06 21/21 passent.
      ✅ DONE 2026-04-12 : 3 corrections dans applications-dependencies.api.spec.ts :
         1. enum invalides ('HTTP'/'API') → 'REST'
         2. auto-liaison RM-01 (sourceAppId==targetAppId) → 2 apps distinctes par test
         3. leading slash sur URLs auth.request ('/applications/...') → sans slash
         100/107 tests passent (6 échecs préexistants : T-007 domains + bugs non liés).

  - id: T-007
    nom: "Tests domains-dependencies bloqués — FS-07 Business Capabilities en cours"
    statut: done
    assigned_agent: qa
    session_active: ~
    date_resolution: "2026-04-12"
    sprint: S3
    theme: "FS-07"
    type: bug
    feature: "FS-07-BACK"
    sessions:
      - tool: CL
        id: "qa-ses"
        nom: "audit tests data-objects"
      - tool: OC
        id: "a1c3e5"
        nom: "fix leading slash + validation 3/3 pass (2026-04-12)"
    notes: |
      Détecté lors de la session QA Data Objects (2026-04-07).
      3 tests Playwright en échec dans tests/domains/domains-dependencies.api.spec.ts :
        - DELETE 409 when applications are linked
        - DELETE 409 when business capabilities are linked
        - DELETE 409 with both apps and business capabilities linked
      Cause : contrainte de suppression domaine↔BC non active tant que FS-07-BACK
      n'est pas en statut done.
      ✅ Gate levée : FS-07-BACK done (2026-04-08). Prêt pour validation par agent QA.
      ✅ DONE 2026-04-12 : fix leading slash dans domains-dependencies.api.spec.ts (3 occurrences
         `/domains/${id}` → `domains/${id}`). Même pattern que T-006 fix #3.
         15/16 tests domains passent (1 échec préexistant T-050 : domains-crud GET array).

  - id: T-008
    nom: "Sécurité — Supprimer fallback JWT hardcodé (F-999 Item 23)"
    statut: done
    assigned_agent: back
    session_active: ~
    date_resolution: "2026-04-12"
    sprint: S3
    theme: "F-999"
    type: bug
    feature: "SECURITY"
    sessions:
      - tool: OC
        id: "b72558"
        nom: "fix getOrThrow + validate() ConfigModule — 96/107 tests API passent"
    notes: |
      F-999 Item 23 — Vulnérabilité confirmée (2026-04-05).
      ✅ DONE 2026-04-12 :
      - jwt.strategy.ts : configService.getOrThrow<string>('JWT_SECRET') (supprime fallback hardcodé)
      - app.module.ts JwtModule : configService.getOrThrow<string>('JWT_SECRET')
      - app.module.ts ConfigModule : validate() built-in — JWT_SECRET requis + min 32 chars (fail-fast au démarrage)
      Validation : make validate-backend ✅ + make test-api-backend 96/107 ✅ (10 échecs pré-existants T-006/T-007).

  - id: T-017
    nom: "Spec FS-07-FRONT Lifecycle tab — Business Capabilities P2"
    statut: done
    assigned_agent: spec
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-12"
    sprint: S4
    theme: "FS-07"
    type: spec
    feature: "FS-07-FRONT-P2"
    sessions:
      - tool: OC
        id: "b3d7a1"
        nom: "spec FS-07-P2-Lifecycle-front — modèle 5 phases + LifecycleStepper"
    notes: |
      Onglet Lifecycle sur le détail d'une Business Capability.
      Visualisation de la trajectoire de modernisation dans le temps.
      Gate : T-018 FS-07-FRONT v1.0 done + décision modèle lifecycle BC (champs dates/phases à définir).

  - id: T-032
    nom: "Audit @Query() sans DTO — vérifier coercition string→number dans tous les controllers NestJS"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: 2026-04-12
    sprint: S3
    theme: "QA"
    type: debt
    feature: "BACK-AUDIT"
    sessions:
      - tool: CL
        id: "f26390"
        nom: "création tâche d'audit suite bug T-031"
      - tool: OC
        id: "c4f9e2"
        nom: "audit + fix it-components @Query plain type"
    notes: |
      Suite au bug T-031 (page/limit reçus comme strings via @Query() sans DTO).
      Passer en revue tous les controllers NestJS qui utilisent @Query() avec un type plain object
      (pas une classe DTO avec @Transform / @Type(() => Number)).
      Vérifier en particulier tous les endpoints de pagination (skip/take Prisma).
      Correctif attendu : remplacer les plain types par des DTOs avec class-transformer,
      ou ajouter Number() dans les services concernés.

  - id: T-048
    nom: "Business Capabilities — Ajout de 3 KPIs en tête de vue liste (niveaux hiérarchiques, applications actives, TBD)"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-12"
    sprint: S4
    theme: "FS-07"
    type: impl
    feature: "FS-07-FRONT"
    sessions:
      - tool: OC
        id: "a3f7c2"
        nom: "impl KpiCard shared + 3 KPIs BC (maxDepth, totalApps, totalBcs)"
    notes: |
      3 cartes KPI occupant chacune 1/3 de la largeur, affichées en tête de la vue liste Business Capabilities.
      KPI 1 : Nombre max de niveaux hiérarchiques (profondeur de l'arbre BC).
      KPI 2 : Nombre total d'applications actives associées aux BC affichées (tenant compte des filtres actifs).
      KPI 3 : À définir — à préciser avec le métier avant implémentation.
      Note : KPI 1 et 2 peuvent nécessiter un endpoint backend ou un calcul côté client depuis les données existantes.

  - id: T-052
    nom: "Amendment FS-07-BACK — Ajouter lifecycleStatus sur BusinessCapability (data + back)"
    statut: done
    assigned_agent: data
    session_active: ~
    handoff_ref: "docs/05-Project/20260412/SESSION-HANDOFF-spec-t017-fs07-lifecycle.md"
    date_resolution: "2026-04-12"
    sprint: S4
    theme: "FS-07"
    type: impl
    feature: "FS-07-BACK"
    sessions:
      - tool: OC
        id: "d4a1f2"
        nom: "schema lifecycleStatus + DTOs + service + db push — 103/107 tests (2026-04-12)"
    notes: |
      Créé par T-017 (spec agent, 2026-04-12) — gate bloquante pour FS-07-P2-FRONT.
      Actions requises :
      1. [data] Ajouter lifecycleStatus String? @map("lifecycle_status") @db.VarChar(50)
         sur le modèle BusinessCapability dans schema.prisma.
         Migration : npx prisma migrate dev --name add_lifecycle_status_to_business_capabilities
      2. [back] Exposer lifecycleStatus dans les DTOs :
         - BusinessCapabilityResponse (GET /:id)
         - BusinessCapabilityListItem (GET / list)
         - BusinessCapabilityTreeNode (GET /tree)
         - UpdateBusinessCapabilityDto (PATCH /:id — champ optionnel nullable)
       Référence spec : docs/03-Features-Spec/P2/FS-07-P2-Lifecycle-front.md §6.

  - id: T-053
    nom: "Frontend — Implémenter FS-07-P2-FRONT (LifecycleStepper + onglet Lifecycle BC)"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: "docs/05-Project/20260412/SESSION-HANDOFF-front-t053-lifecycle-apps.md"
    date_resolution: "2026-04-12"
    sprint: S4
    theme: "FS-07"
    type: impl
    feature: "FS-07-FRONT-P2"
    sessions:
      - tool: OC
        id: "f3a9c1"
        nom: "impl LifecycleStepper + onglets BC (annulé T-056 — mauvaise cible)"
    notes: |
      Gate débloquée par T-052 (2026-04-12) — lifecycleStatus disponible sur BC.
      ⚠️ ANNULÉ par T-056 : implémentation réalisée sur la mauvaise entité (BC au lieu d'Applications).
      Le composant LifecycleStepper.tsx créé ici est réutilisé par T-055 (Applications).
      T-056 retire toutes les modifications BC (front + back + data).

  - id: T-054
    nom: "Backend — Audit endpoint GET /applications/:id : vérifier exposition lifecycleStatus"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-12"
    sprint: S4
    theme: "FS-06"
    type: debt
    feature: "FS-06-BACK"
    sessions:
      - tool: OC
        id: "f1a2b3"
        nom: "audit — RAS, backend déjà correct sur les 3 points (2026-04-12)"
    notes: |
      Contexte : T-055 (front) ajoute le LifecycleStepper sur les pages Application.
      Le backend Applications gère déjà lifecycleStatus (create/update/filter).
      ✅ DONE 2026-04-12 : audit confirmé — aucune modification nécessaire.
      1. GET /api/v1/applications/:id → mapApplicationResponse() inclut lifecycleStatus (service.ts:608)
      2. PATCH /api/v1/applications/:id { lifecycleStatus: null } → update() data ligne 408, @IsOptional() OK
      3. UpdateApplicationDto : @IsString() @IsOptional() lifecycleStatus?: string (dto:65)

  - id: T-055
    nom: "Frontend — LifecycleStepper sur ApplicationDetailPage + ApplicationEditPage"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: "docs/05-Project/20260412/SESSION-HANDOFF-front-t055-lifecycle-detail.md"
    date_resolution: "2026-04-12"
    sprint: S4
    theme: "FS-06"
    type: impl
    feature: "FS-06-FRONT"
    sessions:
      - tool: OC
        id: "f1a2b3"
        nom: "LifecycleStepper DetailPage + fix i18n key + onglet Assessment (2026-04-12)"
    notes: |
      Gate : T-054 done (backend lifecycleStatus Applications vérifié).
      ✅ DONE 2026-04-12 :
      - LifecycleStepper.tsx : fix bug i18n (businessCapabilities.lifecycle → applications.lifecycle)
      - fr.json : ajout common.comingSoon + applications.lifecycle.tabLabel/sectionTitle/notDefined
        tabLabel renommé "Assessment"
      - ApplicationDetailPage : Tabs MUI (Informations | Assessment)
        Tab Informations : LifecycleStepper read-only inline (remplace StatusChip lifecycle)
        Tab Assessment : "À venir" (common.comingSoon)
      ⚠️ Partiel : ApplicationEditPage non traité → voir T-057.

  - id: T-057
    nom: "Frontend — LifecycleStepper éditable dans ApplicationEditPage"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: "docs/05-Project/20260419/SESSION-HANDOFF-front-t057-lifecycle-edit.md"
    date_resolution: "2026-04-19"
    sprint: S4
    theme: "FS-06"
    type: impl
    feature: "FS-06-FRONT"
    sessions:
      - tool: OC
        id: "f9a3c1"
        nom: "impl LifecycleStepper editable + clear button dans ApplicationForm"
    notes: |
      Suite de T-055 (scope déféré).
      Remplacer le <Select> LIFECYCLE_STATUSES dans ApplicationForm.tsx (lignes 626-645)
      par <LifecycleStepper editable onPhaseChange={...} />.
      Ajouter un bouton Clear (lifecycleStatus → null).
      i18n à ajouter : applications.lifecycle.editDescription, clearButton
                       applications.snackbar.lifecycleUpdated
      Gate : aucune (T-054 done, LifecycleStepper disponible).

  - id: T-056
    nom: "Retrait complet lifecycle BC — front + back + data (revert T-052 + T-053)"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: "docs/05-Project/20260412/SESSION-HANDOFF-front-t056-revert-bc.md"
    date_resolution: "2026-04-12"
    sprint: S4
    theme: "FS-07"
    type: debt
    feature: "FS-07-BACK"
    sessions:
      - tool: OC
        id: "c3f8b1"
        nom: "revert complet lifecycle BC — front + back + data + db push (2026-04-12)"
    notes: |
      Revert des implémentations lifecycle sur Business Capabilities (mauvaise cible).
      ✅ DONE 2026-04-12 : retrait 9 fichiers, 103/107 tests passent.
      Couche FRONT (assigned_agent: front) :
        - Supprimer onglets Lifecycle de BusinessCapabilityDetailPage + BusinessCapabilityEditPage
        - Supprimer businessCapabilities.lifecycle.* de fr.json (ajoutés par T-053)
        - Supprimer lifecycleStatus des interfaces BusinessCapability + BusinessCapabilityFormValues
        - Supprimer import Button, ArkAlert, LifecycleStepper inutilisés dans les pages BC
      Couche BACK (même session, agent front peut faire si simple) :
        - Retirer lifecycleStatus de UpdateBusinessCapabilityDto + CreateBusinessCapabilityDto
        - Retirer lifecycleStatus du service BC (findTree select, create data, update data)
      Couche DATA (nécessite accès Docker — à vérifier) :
        - schema.prisma : supprimer lifecycleStatus sur BusinessCapability
        - Migration : npx prisma migrate dev --name remove_lifecycle_status_from_bc
          (exécuter dans le container : docker exec ark-epm-backend-1 ...)
      Note : le composant LifecycleStepper.tsx (components/shared/) est CONSERVÉ — réutilisé par T-055.

  - id: T-058
    nom: "Ajouter scanners sécurité Trivy + Semgrep via commande make (output HTML)"
    statut: done
    assigned_agent: arch
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-19"
    sprint: S4
    theme: "F-999"
    type: impl
    feature: "SECURITY"
    sessions:
      - tool: OC
        id: "f8a2b1"
        nom: "impl Trivy + Semgrep — output HTML via templates"
    notes: |
      ✅ DONE 2026-04-19 :

      Trivy (CVE scanner Docker images) :
      - Template HTML officiel : trivy/html.tpl
      - 4 targets Makefile :
        • scan-backend → reports/trivy/backend.html (5.9 Mo, 2547 vuln)
        • scan-frontend → reports/trivy/frontend.html
        • scan-fs → reports/trivy/fs.html
        • scan-all → les 3 rapports

      Semgrep (SAST scanner code source) :
      - Script conversion JSON→HTML : semgrep/json-to-html.py
      - 3 targets Makefile :
        • semgrep-backend → reports/semgrep/backend.html (5 findings)
        • semgrep-frontend → reports/semgrep/frontend.html (1 finding)
        • semgrep-all → les 2 rapports

      Structure reports/ :
      - reports/trivy/ (gitignored)
      - reports/semgrep/ (gitignored)

      Commit : 1c96b4e — feat(arch): T-058 — Ajout scanners sécurité Trivy + Semgrep avec output HTML

  - id: T-059
    nom: "Spec FS-11 Omnisearch — rédaction specs back et front"
    statut: done
    assigned_agent: spec
    session_active: ~
    handoff_ref: "docs/05-Project/20260419/SESSION-HANDOFF-spec-t059-omnisearch.md"
    date_resolution: "2026-04-19"
    sprint: S4
    theme: "FS-11"
    type: spec
    feature: "FS-11"
    sessions:
      - tool: OC
        id: "c3a7f1"
        nom: "rédaction specs FS-11 Omnisearch back+front (2026-04-19)"
    notes: |
      ✅ DONE 2026-04-19 : specs créées et passées en stable (BACK) / draft (FRONT).
      Fichiers créés :
        - docs/03-Features-Spec/FS-11-omnisearch-back.md (statut: stable)
        - docs/03-Features-Spec/FS-11-omnisearch-front.md (statut: draft — gate FS-11-BACK done)
      Périmètre : recherche transverse sur 7 entités (apps, domains, BC, providers, IT comps, data objects, interfaces).
      Backend : endpoint GET /api/v1/search?q=...&types[]=...&limit=...
      Frontend : composant Omnisearch dans TopBar (Ctrl+K), résultats groupés par type.
      Débloque : T-060 (implémentation backend) et T-061 (implémentation frontend).

  - id: T-060
    nom: "Backend — Implémenter FS-11 Omnisearch (MVP : q, types[], limit, scoring)"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-19"
    sprint: S4
    theme: "FS-11"
    type: impl
    feature: "FS-11-BACK"
    sessions:
      - tool: OC
        id: "b3f7a2"
        nom: "impl SearchModule NestJS MVP (2026-04-19)"
    notes: |
      ✅ DONE 2026-04-19 : SearchModule NestJS implémenté.
      - Endpoint : GET /api/v1/search?q={query}&types[]={type}&limit={n}
      - Scoring : 10/9/5/1 (exact/prefix/substring/description)
      - 7 entités : applications, domains, businessCapabilities, providers, itComponents, dataObjects, interfaces
      - Tests Jest unit : 12/12 pass
      - Build TypeScript : ✅
      - openapi.yaml : ✅ mis à jour avec path /search et schema SearchResultItem
      Hors scope (T-062) : domainIds[], tagIds[], pagination offset.
      Débloque : T-061 (frontend), T-062 (filtres avancés).

  - id: T-061
    nom: "Frontend — Implémenter FS-11 Omnisearch"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-19"
    sprint: S4
    theme: "FS-11"
    type: impl
    feature: "FS-11-FRONT"
    sessions:
      - tool: OC
        id: "8f3a2c"
        nom: "impl FS-11-FRONT Omnisearch — build OK (2026-04-19)"
      - tool: OC
        id: "8f3a2c"
        nom: "amendement US-06 — auto-focus champ recherche (2026-04-19)"
    notes: |
      ✅ Gate levée : T-060 done (2026-04-19) — backend /api/v1/search disponible.
      ✅ Gate levée : T-059 spec FS-11-FRONT stable (passée à stable 2026-04-19).
      Spec : docs/03-Features-Spec/FS-11-omnisearch-front.md (statut: stable).
      ✅ DONE 2026-04-19 : Implémentation Omnisearch frontend complète.
      Fichiers créés :
        - frontend/src/types/search.ts
        - frontend/src/api/search.ts
        - frontend/src/utils/search.utils.tsx
        - frontend/src/components/search/useOmnisearch.ts
        - frontend/src/components/search/OmnisearchItem.tsx
        - frontend/src/components/search/Omnisearch.tsx
        - frontend/src/components/search/index.ts (barrel export)
        - frontend/src/components/shared/KeyboardShortcutHint.tsx
        - e2e/tests/search/omnisearch.spec.ts
      Modifications :
        - frontend/src/components/layout/TopBar.tsx (intégration Omnisearch)
        - frontend/src/i18n/locales/fr.json (clés search.*)
      ✅ Amendement US-06 2026-04-19 : auto-focus à l'ouverture (inputRef + useEffect)
        - Commit : 84991d1
      Validation : build TypeScript ✅, aucun TODO/FIXME.
  - id: T-047
    nom: "Backend — Implémenter filtrage search/domainId dans GET /business-capabilities/tree"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: "docs/05-Project/20260421/SESSION-HANDOFF-back-t047.md"
    date_resolution: "2026-04-21"
    sprint: S4
    theme: "FS-07"
    type: debt
    feature: "FS-07-BACK"
    sessions:
      - tool: CL
        id: "f4a8c2"
        nom: "identifié lors du fix T-041 — filtrage client-side en place en attendant"
      - tool: OC
        id: "d7e9f1"
        nom: "implémenté filtrage serveur search/domainId + élagage arbre + clean front + specs v1.2"
    notes: |
      ✅ Implémenté (T-047, session d7e9f1, 2026-04-21).
      Backend : controller accepte QueryBusinessCapabilitiesDto sur /tree ;
      service findTree(query?) avec algorithme collectKeepIds() :
      nœuds matchants + tous leurs descendants + leurs ancêtres.
      Sans filtre = comportement inchangé (arbre complet).
      Frontend : supprimé filtrage client-side dans flatList useMemo ;
      corrigé logique isTrulyEmpty vs isFilteredEmpty.
      Specs : FS-07-BACK v1.2 (RM-12, 6 cas test), FS-07-FRONT v1.2 (US-16 [x], commentaires).
      ⚠️ RESTANT : les 6 cas Supertest /tree filtré listés dans la spec ne sont
      pas encore écrits en code (backend/test/FS-07-business-capabilities.e2e-spec.ts).
      ⚠️ RESTANT : openapi.yaml pas mis à jour avec les params /tree (NFR-GOV-001).
       (Anciennement T-043 doublon — renommé T-047 lors de la consolidation des archives)

  - id: T-063
    nom: "QA — Intégrer ZAProxy (DAST) dans la pipeline de tests"
    statut: done
    assigned_agent: qa
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-19"
    sprint: S4
    theme: "QA"
    type: impl
    feature: "QA-SECURITY"
    sessions:
      - tool: OC
        id: "f3a9c1"
        nom: "Implémentation ZAP DAST - zap-api-scan + openapi.yaml"
    notes: |
      ✅ Implémentation complète (Makefile targets + config ZAP).
      
      Cibles ajoutées au Makefile :
      - make test-dast : scan API actif avec openapi.yaml (recommandé)
      - make test-dast-baseline : scan passif spider uniquement
      - make open-dast-report : ouverture navigateur rapport
      
      Variables configurables :
      - ZAP_IMAGE=ghcr.io/zaproxy/zaproxy:latest
      - ZAP_TARGET=http://backend:3000/api/v1
      - ZAP_OPENAPI=docs/04-Tech/openapi.yaml
      - Seuil : WARN (Medium+) → échec sur vulnérabilités Medium/High
      
      Pattern : docker run --rm (cohérent avec Trivy/Semgrep)
      Network : ark-epm_default (containers Docker)
      Auth : Header Authorization via get-token.sh
      
      Rapports générés : reports/zap/zap-report.html + .json
      
      ⚠️ NOTE ENV : Environnement Docker local actuellement instable
      (erreur I/O daemon). L'implémentation est validée syntaxiquement
      et prête pour exécution sur environnement Docker healthy.
      
       Pour valider : docker-compose up -d && make test-dast

  - id: T-064
    nom: "Mettre à jour vers Material UI and MUI X v9"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: "docs/05-Project/20260428/SESSION-HANDOFF-front-t064.md"
    date_resolution: "2026-04-28"
    sprint: S4
    theme: "F-999"
    type: debt
    feature: "F-999-FRONT-MUI"
    priorité: medium
    sessions:
      - tool: OC
        id: "b2d4e6"
        nom: "rituel ouverture + migration big-bang MUI v5→v9 + suppression x-tree-view inutilisé"
      - tool: OC
        id: "b2d4e6"
        nom: "fix régression fond bleu drawers + fix débordement ApplicationDetailPage (incomplet)"
      - tool: OC
        id: "a4c9e3"
        nom: "audit impacts + vérification T-071 clôt la régression visuelle restante"

  - id: T-065
    nom: "Retravailler design de la vue détail d'une Application"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: "docs/05-Project/20260421/SESSION-HANDOFF-front-t065.md"
    date_resolution: "2026-04-21"
    sprint: S4
    theme: "FS-06"
    type: impl
    feature: "FS-06-FRONT"
    sessions:
      - tool: OC
        id: "b3f7a2"
        nom: "Redesign ApplicationDetailPage — 2-col layout, 3 tabs, header enrichi"

  - id: T-066
    nom: "openapi.yaml à mettre à jour sur tous les endpoints"
    statut: done
    assigned_agent: arch
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-29"
    sprint: S4
    theme: "F-999"
    type: debt
    feature: "F-999-API-CONTRACT"
    priorité: medium
    sessions:
      - tool: CL
        id: "c7d4e2"
        nom: "audit complet + fix indentation + ajout /users /roles /permissions (v1.5.0)"
      - tool: CL
        id: "c7d4e2"
        nom: "corrections divergences majeurs : tags réécrits, /domains paginé, DELETE roles 200 (v1.6.0)"
    notes: |
      ✅ Implémenté (session c7d4e2, 2026-04-29) — openapi.yaml v1.6.0.

      Round 1 (v1.5.0) :
      1. /business-capabilities/tree : ajout params search + domainId (T-047 RESTANT levé).
      2. Bugs indentation YAML :
         - Suppression doublon ApplicationListItem mal niché dans UpdateITComponentDto.
         - Correction ApplicationResponse, CreateApplicationDto, UpdateApplicationDto :
           itComponents / businessCapabilities / owner / capabilityIds / ownerId
           ramenés au bon niveau (propriétés de l'objet, pas sous providers).
      3. Modules RBAC entiers ajoutés :
         - /users (GET, POST, GET/:id, PATCH/:id, DELETE/:id soft-delete)
         - /roles (GET, POST, GET/:id, PATCH/:id, DELETE/:id, PUT/:id/permissions)
         - /permissions (GET, POST)
         Schémas : CreateUserDto, UpdateUserDto, RoleListItem, CreateRoleDto,
         UpdateRoleDto, PermissionResponse, CreatePermissionDto.

      Round 2 (v1.6.0) — corrections divergences backend/openapi :
      4. GET /domains : array brut → { data, meta } paginé + 5 query params
         (page, limit, search, sortBy[name|description|createdAt], sortOrder).
      5. Section tags entièrement réécrite :
         - /tags/dimensions → /tag-dimensions (chemin réel)
         - GET /tag-dimensions : ajout param entityType
         - PATCH /tag-dimensions/{id} ajouté (UpdateTagDimensionDto)
         - /tags/dimensions/{id}/autocomplete → GET /tags/autocomplete?dimension=&query=&limit=
         - /tags/dimensions/{id}/resolve → POST /tags/resolve (body ResolveTagDto)
         - /tags/entities/{type}/{id} → /tags/entity/{type}/{id} (singulier)
         - PUT /tags/entity/{type}/{id}/batch ajouté (BatchEntityTagsDto)
         Schémas : UpdateTagDimensionDto, ResolveTagDto, PutEntityTagsDto, BatchEntityTagsDto.
      6. DELETE /roles/:id : 204 → 200 (pas de @HttpCode backend).
      Tests Playwright pour /users /roles /permissions → T-078.

  - id: T-067
    nom: "Horodater tous les rapports de tests dans /reports"
    statut: done
    assigned_agent: qa
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-23"
    sprint: S4
    theme: "QA"
    type: impl
    feature: "QA-REPORTS"
    sessions:
      - tool: OC
        id: "2e64f9"
        nom: "rituel ouverture + impl horodatage rapports"
    notes: |
      ✅ Implémenté (session 2e64f9, 2026-04-22).
      Format : préfixe timestamp YYYYMMDD-HHMMSS au début du nom de fichier.
      Outils couverts :
      - Trivy (scan-backend, scan-frontend, scan-fs) → reports/trivy/YYYYmmdd-HHMMSS-{backend,frontend,fs}.html
      - Semgrep (semgrep-backend, semgrep-frontend) → reports/semgrep/YYYYmmdd-HHMMSS-{backend,frontend}.{html,json}
      - ZAP (test-dast, test-dast-baseline) → reports/zap/YYYYmmdd-HHMMSS-{zap,baseline}-report.{html,json}
      open-dast-report : ouvre le rapport HTML le plus récent (ls -t).
      openapi-zap.yaml : fichier temporaire supprimé après chaque run test-dast.
      Pas de symlink, pas de purge auto (conforme demande).
      e2e/reports/ : hors périmètre — nouvelle tâche à créer pour déporter les rapports Playwright dans reports/.

  - id: T-068
    nom: "fix bug 'make test-dast' qui ne produit rien"
    statut: done
    assigned_agent: qa
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-22"
    sprint: S4
    theme: "QA"
    type: bug
    feature: "QA-SECURITY"
    sessions:
       - tool: OC
         id: "e8c4a2"
         nom: "fix Makefile test-dast + baseline + open-dast-report"

  - id: T-070
    nom: "QA — Implémenter MegaLinter dans la suite des outils de qualité"
    statut: done
    assigned_agent: qa
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-23"
    sprint: S4
    theme: "QA"
    type: impl
    feature: "QA-MEGALINTER"
    priorité: medium
    sessions:
      - tool: OC
        id: "d4e5f6"
        nom: "Implémentation MegaLinter — config, Makefile, NFR"
      - tool: CL
        id: "a3f7c9"
        nom: "refonte config qualité: JSCPD, ESLint v8 compat, targets megalinter-*, analyse findings"
    notes: |
      ✅ Implémenté (session d4e5f6, 2026-04-23). Refondu (session a3f7c9, 2026-04-29).

      Config initiale (d4e5f6) : 7 linters, 0 fichiers analysés (bug ENABLE vs ENABLE_LINTERS).

      Refonte (a3f7c9) — objectif qualité code backend/src + frontend/src :
      - ENABLE → ENABLE_LINTERS (correction bug activation)
      - Linters retenus : TYPESCRIPT_ES + COPYPASTE_JSCPD + DOCKERFILE_HADOLINT
      - TYPESCRIPT_ES : ESLint v8 (megalinter:v8) incompatible flat config ESLint v9
        → backend/.eslintrc.cjs créé (legacy, compatible ESLint v8), eslint.config.mjs préservé pour dev local
      - COPYPASTE_JSCPD : détection duplication backend/src + frontend/src (.jscpd.json)
      - .mega-linter-config.yml : cible séparée YAML/JSON/ENV (make megalinter-config)
      - Makefile targets : megalinter-quality / megalinter-config / megalinter / megalinter-report

      Résultats premier run complet (2026-04-29) :
      - TYPESCRIPT_ES : 202 fichiers analysés, 1 bug réel (T-081), ~65 warnings no-unused-vars
      - COPYPASTE_JSCPD : doublons ProviderEditPage/ProviderNewPage confirmés
      - DOCKERFILE_HADOLINT : 1 finding (e2e/Dockerfile npm sans version pinée)

  - id: T-071
    nom: "Frontend — Audit + fix Grids imbriqués sur toutes les pages (follow-up T-064)"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-28"
    sprint: S4
    theme: "F-999"
    type: debt
    feature: "F-999-FRONT-MUI"
    priorité: medium
    sessions:
      - tool: OC
        id: "f1a3b5"
        nom: "audit Grid→Box flex — 3 fichiers, 0 erreur tsc"
    notes: |
      ✅ Implémenté (session f1a3b5, 2026-04-28).
      Audit : seuls 3 fichiers avaient des Grid problématiques (6 autres pages = pas de Grid).
      ApplicationDetailPage.tsx : Grid 8/4 → Box flex 2:1, overflow fix RelationCard/ClickableRow/Chip.
      BusinessCapabilitiesPage.tsx : KPI band Grid 3×sm:4 → Box flex 1:1:1 (bug stacking gauche corrigé).
      BusinessCapabilityMatrix.tsx : root Grid container + nested Grid container (récursif) → Box flexWrap.
        Breakpoint depth=0 : sm → md (sm donnait ~148px/carte avec sidebar 240px — trop étroit).
      Import Grid supprimé des 3 fichiers. tsc exit:0, Vite build ✅.

  - id: T-072
    nom: "Docs — Mise à jour documentation post-migration MUI v9 (19 fichiers)"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-28"
    sprint: S4
    theme: "F-999"
    type: doc
    feature: "F-999-FRONT-MUI"
    priorité: medium
    sessions:
      - tool: OC
        id: "a4c9e3"
        nom: "audit impacts MUI v9 + mise à jour 19 fichiers docs/specs/AGENTS"
    notes: |
      ✅ Terminé (session a4c9e3, 2026-04-28). Follow-up doc de T-064 + T-071.
      Fichiers mis à jour (MUI v5 → v9 + patterns Grid→Box) :
      - AGENTS.md (racine) : MUI v5 → v9 ×3
      - frontend/AGENTS.md : MUI v5 → v9 ×2 + anti-patterns 6 (Grid imbriqué) & 7 (props renommées)
      - docs/03-Features-Spec/_templates/_template_front.md : MUI v5 → v9 ×3
      - docs/03-Features-Spec/F-foundations/F01-Design-System.md : MUI v5 → v9
      - docs/03-Features-Spec/F-foundations/F03-Dimension-Tag-Foundation.md : MUI v5 → v9 ×5
      - docs/03-Features-Spec/FS-02/03/04/05/06/07/08/11 front specs : MUI v5 → v9
      - docs/03-Features-Spec/FS-06 : Grid YAML → Box flex ×3
      - docs/03-Features-Spec/FS-07 : Grid code → Box flex + note @mui/x-tree-view supprimé
      - docs/03-Features-Spec/P2/FS21 + FS-07-P2 : MUI v5 → v9
      - docs/04-Tech/ARK-Architecture-Setup.md : install cmd complété (@mui/icons-material, x-date-pickers, # MUI v9)
      - docs/05-Project/README.md + REX/ark-doc-system.md+html + REX/ark-rex-workflow.md : MUI v5 → v9
      Zéro résiduel MUI v5 dans les fichiers opérationnels (SESSION-HANDOFF archives + tasks.yaml notes exclus).

  - id: T-073
    nom: "Adapter le layout de Application Detail aux autres pages détail (Business Cap, Interface, Data Object, IT Component, Fournisseur)"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: "docs/05-Project/20260428/SESSION-HANDOFF-front-t073.md"
    date_resolution: "2026-04-28"
    sprint: S4
    theme: "F-999"
    type: impl
    feature: "F-999-FRONT-DETAIL-LAYOUT"
    priorité: medium
    sessions:
      - tool: OC
        id: "a7b3c9"
        nom: "refonte layout 2-col + Avatar + Tabs sur 5 pages détail + specs"
    notes: |
      ✅ Implémenté (session a7b3c9, 2026-04-28).
      Pages refondues : BusinessCapabilityDetail, InterfaceDetail,
      DataObjectDetail, ITComponentDetail, ProviderDetail.
      Pattern appliqué (référence ApplicationDetailPage T-065) :
      - maxWidth="xl" sur toutes les pages
      - Header inline : Avatar (couleur entité) + h2 + sous-titre + chips + Edit
      - Tabs : Informations (2-col flex 2:1) + Applications (table)
      - Footer : Retour à gauche + Delete à droite (si applicable)
      - AppBreadcrumbs sur toutes les pages (PNS-11)
      Composants partagés extraits : DetailComponents.tsx (DetailRow, ClickableRow, RelationCard)
      i18n : +2 clés (businessCapabilities.detail.updatedAt, existantes réutilisées)
      Specs mises à jour : FS-07 §4.6, FS-08 §4.3, FS-03 §4.3, FS-04 §4.3, FS-05 §4.3
      Validation : tsc 0 erreur, npm run build OK.

  - id: T-081
    nom: "Back — Corriger no-control-regex dans tags.service.ts (détecté MegaLinter)"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: 2026-04-29
    sprint: S4
    theme: "QA"
    type: bug
    feature: "F-03-TAGS"
    priorité: high
    sessions:
      - tool: CL
        id: "a3f7c9"
        nom: "détecté par ESLint (MegaLinter) lors de la session qualité"
      - tool: OC
        id: "b9e2d1"
        nom: "refactor hasInvalidPathChars() via charCodeAt() — regex supprimée"
    notes: |
      ESLint rule no-control-regex — backend/src/tags/tags.service.ts:15
      Regex contenant des caractères de contrôle (\x00, \x1f) — potentiellement dangereux.
      Commande : make megalinter-quality → ERROR-TYPESCRIPT_ES.log pour détail.
      Fix : const INVALID_PATH_CHARS supprimée, remplacée par hasInvalidPathChars(str)
      utilisant charCodeAt() avec bornes ASCII explicitement documentées (0–31, 34, 39, 60, 62, 92).
