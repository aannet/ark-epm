tasks:
  - id: T-001
    nom: "Spec FS-06 Applications — backend"
    statut: done
    date_resolution: "2026-03-29"
    sprint: S1
    theme: "FS-06"
    type: spec
    feature: "FS-06-BACK"
    sessions:
      - tool: OC
        id: "a1b2c3"
        nom: "spec applications back"
    notes: "Spec NestJS complète : CRUD + RBAC + audit trail + pagination cursor"

  - id: T-002
    nom: "Choix dnd-kit vs MUI X pour drag-and-drop tags"
    statut: done
    date_resolution: "2026-04-05"
    sprint: S1
    theme: "F-03"
    type: decision
    feature: "F-03"
    sessions:
      - tool: OC
        id: "a3f9d1"
        nom: "dnd-kit decision"
      - tool: CL
        id: "b88c12"
        nom: "archi tags FS-21"
    notes: "Tranché → @dnd-kit (MIT), plus léger que MUI X Tree View pour ce besoin"

  - id: T-004
    nom: "Bug — Tri par nom non fonctionnel sur GET /applications"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: "docs/05-Project/20260408/SESSION-HANDOFF-back-t004-recette-fs07.md"
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-06"
    type: bug
    feature: "FS-06-BACK"
    sessions:
      - tool: CL
        id: "qa-ses"
        nom: "audit tests data-objects"
      - tool: OC
        id: "1dde61"
        nom: "fix sortBy=name test limit in Playwright"
    notes: |
      Détecté lors de la session QA Data Objects (2026-04-07).
      Test Playwright : tests/applications/applications-pagination.api.spec.ts:50.
      GET /applications?sortBy=name&sortOrder=asc — réponse 200 mais ordre non respecté.
      À corriger par agent back (ApplicationsService.findAll — clause orderBy).
      Résolution (2026-04-08) : le tri backend est correct, le test ne forçait pas la pagination.
      Root cause : `limit` implicite à 20, donc "Zebra App" pouvait être hors page 1.
      Fix : ajout de `limit=100` dans `e2e/tests/applications/applications-pagination.api.spec.ts`.
      Vérification : `playwright test --project=api-backend tests/applications/applications-pagination.api.spec.ts` → PASS (5) FAIL (0).

  - id: T-005
    nom: "Bug — Isolation données entre tests IT Components (DELETE 204/409)"
    statut: done
    assigned_agent: qa
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "QA"
    type: bug
    feature: "QA-IT-COMPONENTS"
    sessions:
      - tool: CL
        id: "qa-ses"
        nom: "audit tests data-objects"
      - tool: CL
        id: "enc-sq"
        nom: "fix cleanup Playwright — itComponents detach"
    notes: |
      Détecté lors de la session QA Data Objects (2026-04-07).
      Corrigé 2026-04-08 : test-data.fixture.ts createApplication cleanup —
      PATCH itComponents:[] avant DELETE (détache les ICs sans bloquer la suppression).
      Root cause : cleanup LIFO tentait DELETE app avant que les ICs soient nettoyés,
      hasDependencies=true → skip → app + domain orphelins.

  - id: T-006
    nom: "Tests applications-dependencies bloqués — FS-08 Interfaces non implémenté"
    statut: open
    assigned_agent: qa
    session_active: ~
    date_resolution: ~
    sprint: S4
    theme: "FS-08"
    type: bug
    feature: "FS-08-BACK"
    sessions:
      - tool: CL
        id: "qa-ses"
        nom: "audit tests data-objects"
      - tool: OC
        id: "a9f3e1"
        nom: "déblocage T-006 après T-040 FS-08-BACK done"
    notes: |
      Détecté lors de la session QA Data Objects (2026-04-07).
      4 tests Playwright en échec dans tests/applications/applications-dependencies.api.spec.ts :
        - DELETE 409 when application has interfaces linked
        - DELETE 409 with detailed counts
        - GET /dependencies hasDependencies=true when linked
        - GET /dependencies hasDependencies=false for isolated app
      ✅ Gate levée : FS-08-BACK done (T-040, 2026-04-12).
      Prêt pour validation par agent QA — la table interfaces et les relations Prisma sont en place.

  - id: T-007
    nom: "Tests domains-dependencies bloqués — FS-07 Business Capabilities en cours"
    statut: blocked
    assigned_agent: qa
    session_active: ~
    date_resolution: ~
    sprint: S3
    theme: "FS-07"
    type: bug
    feature: "FS-07-BACK"
    sessions:
      - tool: CL
        id: "qa-ses"
        nom: "audit tests data-objects"
    notes: |
      Détecté lors de la session QA Data Objects (2026-04-07).
      3 tests Playwright en échec dans tests/domains/domains-dependencies.api.spec.ts :
        - DELETE 409 when applications are linked
        - DELETE 409 when business capabilities are linked
        - DELETE 409 with both apps and business capabilities linked
      Cause : contrainte de suppression domaine↔BC non active tant que FS-07-BACK
      n'est pas en statut done. Gate : FS-07-BACK done.

  - id: T-003
    nom: "Implémenter FS-09 Dependency Graph — frontend ReactFlow"
    statut: blocked
    assigned_agent: front
    session_active: ~
    date_resolution: ~
    sprint: S2
    theme: "FS-09"
    type: impl
    feature: "FS-09-FRONT"
    sessions:
      - tool: OC
        id: "a1b2c3"
        nom: "rituel ouverture - gate bloqué : spec vide"
    notes: |
      GATE BLOQUÉ : Spec FS-09-Dependency-Graph.md est vide (0 lignes).
      Prérequis : FS-09-BACK spec + impl done, FS-09-FRONT spec stable.
      Action requise : Agent `spec` doit rédiger la spec FS-09 avant implémentation.

  - id: T-008
    nom: "Sécurité — Supprimer fallback JWT hardcodé (F-999 Item 23)"
    statut: open
    assigned_agent: back
    session_active: ~
    date_resolution: ~
    sprint: S3
    theme: "F-999"
    type: bug
    feature: "SECURITY"
    sessions: []
    notes: |
      F-999 Item 23 — Vulnérabilité confirmée (2026-04-05).
      backend/src/auth/jwt.strategy.ts : configService.get('JWT_SECRET') || 'fallback-secret-do-not-use-in-prod'
      Action : remplacer par configService.getOrThrow<string>('JWT_SECRET')
      Ajouter validation Joi dans ConfigModule (JWT_SECRET min 32 chars, required).
      Bloquant avant toute mise en production.

  - id: T-009
    nom: "Migration tests Cypress → Playwright — Data Objects (F-999 Item 22)"
    statut: open
    assigned_agent: qa
    session_active: ~
    date_resolution: ~
    sprint: S3
    theme: "F-999"
    type: impl
    feature: "QA-DATA-OBJECTS"
    sessions: []
    notes: |
      F-999 Item 22 — 37 tests Cypress (frontend/cypress/e2e/data-objects.cy.ts) à migrer.
      Destination : e2e/tests/data-objects/data-objects.spec.ts (Playwright).
      Cypress inutilisable sur cet env (dépendances libnspr4/X11 manquantes).
      Après migration : supprimer frontend/cypress/ et retirer dépendances Cypress de package.json.

  - id: T-010
    nom: "Implémenter FS-05-FRONT — Data Objects frontend"
    statut: done
    assigned_agent: front
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-05"
    type: impl
    feature: "FS-05-FRONT"
    sessions:
      - tool: CL
        id: "ark-osl"
        nom: "audit statut FS-05-FRONT — correction tracking"
    notes: |
      Spec FS-05-FRONT v0.1 stable — PNS-02 drawer, Chip isSourceOfTruth (success/default),
      Autocomplete type (freeSolo + database/dataset/file), colonne role (consumer/producer/owner).
      Gate : FS-05-BACK done ✅. Amendements A+B backend (isSourceOfTruth, type autocomplete) pré-requis.
      Implémentation confirmée 2026-04-08 : 10 fichiers frontend (pages, composants, API client,
      types, utils), 4 routes câblées dans App.tsx, toutes les US réalisées.

  - id: T-011
    nom: "Implémenter FS-07-BACK — Business Capabilities backend"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: "SESSION-HANDOFF.md"
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-07"
    type: impl
    feature: "FS-07-BACK"
    sessions:
      - tool: OC
        id: "d01cbc"
        nom: "impl FS-07-BACK business capabilities"
      - tool: OC
        id: "4babfd"
        nom: "Gate G-17 — openapi.yaml BC endpoints + schemas"
    notes: |
      Spec FS-07-Business-Capabilities-back.md v1.0 stable.
      Hiérarchie récursive WITH RECURSIVE, level auto-calculé, hiérarchie illimitée, prévention circulaire,
      N:N avec Applications, migration NFR-GOV-005.
      
      Implémentation complète (2026-04-08) :
      - Module NestJS (controller, service, DTOs, module, index)
      - 8 endpoints : CRUD + /tree + /:id/children + /:id/applications
      - Helpers : calculateLevel(), isDescendant(), recalculateLevelsRecursively()
      - 19 tests Jest unitaires
      - 20 tests Supertest e2e
      - 21 tests Playwright API (crud, validation, tree)
      - Seed 12 business capabilities (3 niveaux hiérarchiques)
      - Fix HttpExceptionFilter : propagation du champ 'details' dans les erreurs
      
      Gate G-17 validée (2026-04-08) : openapi.yaml mis à jour avec 8 paths + 5 schemas BC.
      Gate T-007 (tests domains-dependencies) débloquée.

  - id: T-012
    nom: "Débloquer sélecteur Owner — API Users mock (F-999 Item 12b)"
    statut: blocked
    assigned_agent: back
    session_active: ~
    date_resolution: ~
    sprint: S3
    theme: "F-999"
    type: bug
    feature: "FS-06-FRONT"
    sessions: []
    notes: |
      F-999 Item 12b — MOCK_USERS = [] toujours hardcodé dans ApplicationNewPage/EditPage.
      Sélecteur owner vide dans formulaire Application.
      Déblocage : endpoint GET /api/v1/users?isActive=true nécessaire.
      Migration : créer hook useUsers() dans frontend/src/api/users.ts (pattern useProviders).
      Gate : endpoint users disponible (FS-01 ou module dédié).

  - id: T-013
    nom: "Fix cleanup Playwright — tests + global teardown"
    statut: done
    assigned_agent: qa
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "QA"
    type: impl
    feature: "QA-CLEANUP"
    sessions:
      - tool: CL
        id: "enc-sq"
        nom: "fix cleanup Playwright complet"
    notes: |
      Deux root causes corrigées :
      1. Applications créées via auth.request.post() directement (non traquées) →
         converties vers testData.createApplication() dans :
         - applications-crud.api.spec.ts (6 tests)
         - applications-validation.api.spec.ts (duplicate name test)
         - domains-crud.api.spec.ts (POST create domain)
      2. createApplication cleanup — PATCH itComponents:[] + DELETE inconditionnels
         (supprime le check hasDependencies qui bloquait le nettoyage).
      Ajout : e2e/global-teardown.ts + playwright.config.ts globalTeardown
      → détecte automatiquement les fuites post-run (log par préfixe de nom).

  - id: T-014
    nom: "Seed domaines + applications — affectation cohérente"
    statut: done
    assigned_agent: data
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "F-999"
    type: impl
    feature: "SEED"
    sessions:
      - tool: CL
        id: "enc-sq"
        nom: "seed domains + apps affectation cohérente"
    notes: |
      seed-applications.ts : ajout champ domainName par application,
      lookup par Map<name, domain> au lieu d'affectation aléatoire.
      10 domaines insérés via seed-domains.ts.
      25 applications insérées via seed-applications.ts.
      Répartition : Finance (3), IT (13), RH (1), Ventes (2),
      Marketing (1), Supply Chain (1), R&D (1), Service Client (1), Prod (0), Juridique (0).
      Purge préalable : 105 apps + 111 domaines + 84 IT components résidus tests supprimés via SQL.

  - id: T-015
    nom: "Tests UI Playwright — Business Capabilities frontend"
    statut: done
    assigned_agent: qa
    session_active: ~
    handoff_ref: "docs/05-Project/20260409/SESSION-HANDOFF-qa-t015-playwright-fs07.md"
    date_resolution: "2026-04-09"
    sprint: S3
    theme: "FS-07"
    type: test
    feature: "FS-07-FRONT"
    sessions:
      - tool: OC
        id: "7d2b4c"
        nom: "playwright ui fs-07 + stabilisation env local"
    notes: |
      Tests UI Playwright pour l'écran Business Capabilities (arbre hiérarchique, CRUD UI).
      Gate : FS-07-BACK done (T-011) ✅ + FS-07-FRONT implémenté (T-020) ✅.
      Fixture createBusinessCapability() déjà disponible dans e2e/fixtures/test-data.fixture.ts.
      Débloqué 2026-04-08 après T-020 done.

  - id: T-016
    nom: "Spec FS-07-FRONT — Business Capabilities frontend"
    statut: done
    assigned_agent: spec
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-07"
    type: spec
    feature: "FS-07-FRONT"
    sessions:
      - tool: CL
        id: "6b4a8f"
        nom: "rédaction spec FS-07-FRONT v1.0"
    notes: |
      Spec frontend Business Capabilities : 3 vues (liste arborescente, arbre MUI TreeView, matrix),
      drawer read-only, formulaire avec criticality/technicalFit, agrégation récursive client-side.
      US01-05, US06/07 révisées, US09, US10, US13, US16, US19.
      Gate : T-019 amendment backend (criticality + technicalFit) done avant impl T-018.
      
      Spec v1.0 rédigée (1000+ lignes) : 13 sections, 8 composants, 7 règles métier frontend.
      Amendment T-019 documenté dans FS-07-BACK v1.1 (Prisma + OpenAPI + migration SQL).
      Décisions architecture : D-01 (enums scalaires), D-02 (MUI TreeView), D-03 (agrégation client-side),
      D-04 (US06 reformulée), D-05 (AppBreadcrumbs PNS-11).

  - id: T-017
    nom: "Spec FS-07-FRONT Lifecycle tab — Business Capabilities P2"
    statut: open
    assigned_agent: spec
    session_active: ~
    date_resolution: ~
    sprint: S4
    theme: "FS-07"
    type: spec
    feature: "FS-07-FRONT-P2"
    sessions: []
    notes: |
      Onglet Lifecycle sur le détail d'une Business Capability.
      Visualisation de la trajectoire de modernisation dans le temps.
      Gate : T-018 FS-07-FRONT v1.0 done + décision modèle lifecycle BC (champs dates/phases à définir).

  - id: T-018
    nom: "Implémenter FS-07-FRONT phase 1 — Fondation + chips"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: "docs/05-Project/20260408/SESSION-HANDOFF-front-fs07-phase1.md"
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-07"
    type: impl
    feature: "FS-07-FRONT"
    sessions:
      - tool: OC
        id: "e4a7f2"
        nom: "fondation FS-07-FRONT (types, API, utils, chips)"
    notes: |
      Phase 1 complète : fondation technique + 3 composants chips.
      
      Fichiers créés :
      - Types : businessCapability.ts (CriticalityLevel, TechnicalFitLevel, BusinessCapability, TreeNode, FormValues, ApplicationMapping, ViewMode)
      - API : businessCapabilities.ts (8 hooks React Query)
      - Utils : businessCapability.utils.ts (sumApplications, isDescendant, buildHierarchyPath, flattenTree, getRootNodeIds)
      - Composants : CriticalityChip, TechnicalFitChip, AppLifecycleBreakdown
      - AppBreadcrumbs : déjà existant, conforme PNS-11
      
      Reste à faire (T-020) :
      - 5 composants : Form, Drawer, Tree, Matrix + installer @mui/x-tree-view
      - 4 pages : List, New, Detail, Edit
      - Clés i18n section businessCapabilities
      - Tests manuels checklist §12
      
      Gates : T-016 spec stable + T-019 amendment backend done.

  - id: T-019
    nom: "Amendment FS-07-BACK — champs criticality + technicalFit"
    statut: done
    assigned_agent: back
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-07"
    type: impl
    feature: "FS-07-BACK"
    sessions:
      - tool: OC
        id: b87ec4
        nom: "amendment backend criticality + technicalFit"
    notes: |
      Ajout 2 champs enum sur BusinessCapability : criticality (LOW/MEDIUM/HIGH/CRITICAL),
      technicalFit (ADEQUATE/PARTIAL/INADEQUATE/LEGACY).
      Migration SQL (CREATE TYPE + ALTER TABLE), Prisma model, DTOs (Create/Update/Response/ListItem/TreeNode),
      seed (12 capabilities alimentées avec valeurs réalistes), tests manuels validés.
      Gate T-018 (impl FS-07-FRONT) débloquée.

  - id: T-020
    nom: "Implémenter FS-07-FRONT phase 2 — Composants + pages + i18n"
    statut: done
    assigned_agent: front
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-07"
    type: impl
    feature: "FS-07-FRONT"
    sessions:
      - tool: OC
        id: "5ecde5"
        nom: "impl FS-07-FRONT phase 2 complète"
    notes: |
      Phase 2 complète : 5 composants + 4 pages + i18n + routes.
      
      Composants créés (5) :
      1. BusinessCapabilityForm (validation client-side, exclusion descendants via isDescendant, Select criticality/technicalFit)
      2. BusinessCapabilityDrawer (PNS-02 read-only, breadcrumb hiérarchique via buildHierarchyPath, tabs Info/Applications, AppLifecycleBreakdown)
      3. BusinessCapabilityTree (MUI x-tree-view SimpleTreeView + TreeItem, expand/collapse natif, CriticalityChip inline)
      4. BusinessCapabilityMatrix (tuiles imbriquées Grid + Card, coloration getCriticalityColor, sumApplications recursive, Tooltip breakdown)
      5. getCriticalityColor util ajouté dans businessCapability.utils.ts
      
      Pages créées (4) :
      1. BusinessCapabilitiesPage (liste arborescente flat + vue tree + vue matrix, ToggleButtonGroup, filtres search+domain, drawer)
      2. BusinessCapabilityNewPage (AppBreadcrumbs PNS-11, BusinessCapabilityForm, tagsApi.getDimensions)
      3. BusinessCapabilityDetailPage (read-only, CriticalityChip, TechnicalFitChip, TagChipList, navigate on parent click)
      4. BusinessCapabilityEditPage (3 tabs Général/Relations/Audit, reparenting inline, mini-table applications, EmptyState audit P2)
      
      i18n : section businessCapabilities (~100 clés) ajoutée dans fr.json
      
      Routes : câblées dans App.tsx (/business-capabilities, /new, /:id, /:id/edit)
      
      Extension PageHeader : ajout prop secondaryAction pour le ToggleButtonGroup
      
      Dépendance : @mui/x-tree-view installé
      
      TypeScript : 0 erreurs spécifiques aux fichiers FS-07-FRONT (erreurs préexistantes dans IT-Components/Applications non liées)

  - id: T-021
    nom: "Fix YAML indentation openapi.yaml — dette technique"
    statut: done
    assigned_agent: arch
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "F-999"
    type: debt
    feature: "OPENAPI"
    sessions:
      - tool: OC
        id: "4babfd"
        nom: "fix 457 lignes indentation impaire → paire"
    notes: |
      openapi.yaml avait 457 lignes avec indentation impaire (1, 3, 5, 7+ espaces)
      au lieu de l'indentation standard 2-espaces YAML.
      Python yaml.safe_load() rejetait le fichier entier.
      
      Correction : normalisation de toutes les lignes à indentation paire.
      Paths affectés : /providers/{id}/applications, /applications, /applications/{id},
      /applications/{id}/dependencies, /applications/{id}/it-components, /it-components.
      
      Commit : 4babfd7 (inclus avec Gate G-17 FS-07-BACK).

  - id: T-022
    nom: "Fix erreurs TypeScript préexistantes — IT Components + Applications"
    statut: done
    assigned_agent: front
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "F-999"
    type: debt
    feature: "TS-FIX"
    sessions:
      - tool: OC
        id: "5ecde5"
        nom: "fix 5 erreurs TS ITComponent + Application"
      - tool: OC
        id: "c3f8a1"
        nom: "stabilisation FS-07-FRONT bugfix session"
    notes: |
      5 erreurs TypeScript préexistantes corrigées (bloquaient npm run build) :
      1. ITComponentDrawer.tsx:56 — EntityTagResponse[] → TagValueResponse[] mapping inline
      2. ITComponentListPage.tsx:176 — EntityTagResponse[] → TagValueResponse[] mapping inline
      3. ApplicationNewPage.tsx:135 — PaginatedResponse<Domain> → SelectOption[] (domainOptions)
      4. ApplicationEditPage.tsx:158 — PaginatedResponse<Domain> → SelectOption[] (domainOptions)
      5. ITComponentFormPage.tsx:128 — technology: null → undefined (mapping explicite)
      
      Détectées lors de T-020 (FS-07-FRONT phase 2).
      Root cause : types API (EntityTagResponse, PaginatedResponse) vs types formulaires (TagValueResponse, SelectOption).

  - id: T-023
    nom: "Fix API businessCapabilities/tree — response.data.data extraction"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: "docs/05-Project/20260408/SESSION-HANDOFF-front-fs07-bugfix.md"
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-07"
    type: bug
    feature: "FS-07-FRONT"
    sessions:
      - tool: OC
        id: "c3f8a1"
        nom: "fix useBusinessCapabilitiesTree response extraction"
    notes: |
      Bug : nodes.reduce is not a function sur la page Business Capabilities.
      Root cause : GET /business-capabilities/tree retourne { data: [...] }
      mais useBusinessCapabilitiesTree retournait response.data (= { data: [...] })
      au lieu de response.data.data (= [...]).
      
      Fix : frontend/src/api/businessCapabilities.ts ligne 56-59
      - Avant : client.get<BusinessCapabilityTreeNode[]>(...) + return response.data
      - Après : client.get<{ data: BusinessCapabilityTreeNode[] }>(...) + return response.data.data
      
      Également résolu cette session :
      - Installation @mui/x-tree-view dans container Docker (npm install)
      - Restart container frontend
      - Fix 5 erreurs TypeScript préexistantes (T-022)

  - id: T-024
    nom: "Recette FS-07-FRONT — Business Capabilities frontend"
    statut: done
    assigned_agent: qa
    session_active: ~
    handoff_ref: "docs/05-Project/20260408/SESSION-HANDOFF-qa-t024-recette-fs07.md"
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-07"
    type: review
    feature: "FS-07-FRONT"
    sessions:
      - tool: OC
        id: "f8a3d1"
        nom: "recette statique fs-07 + hotfix conformité"
    notes: |
      Phase de recette de l'implémentation frontend Business Capabilities (T-018, T-020).
      Valider la checklist §12 de la spec FS-07-BACK (synthèse : 3 vues, PNS-11, toggle/view switching,
      expand/collapse, drawer PNS-02, CriticalityChip + TechnicalFitChip, agrégation récursive,
      erreurs 400 CIRCULAR_REFERENCE / 409 DEPENDENCY_CONFLICT).

      Gates considérées : FS-07-FRONT implémentée (T-020) + APIs FS-07-BACK et amendements (T-019).
      Lié à la livraison : T-024.

  - id: T-025
    nom: "Amendment FS-06-BACK — capabilityIds dans DTOs + sync app_capability_map"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: "docs/05-Project/20260408/SESSION-HANDOFF-back-t025-fs06-bc.md"
    date_resolution: 2026-04-08
    sprint: S3
    theme: FS-07
    type: impl
    feature: FS-06-BACK
    sessions:
      - tool: OC
        id: "b4ca76"
        nom: "impl capabilityIds + validation build/e2e 21/21 + commit"
    notes: |
      Amendment FS-06-BACK pour implémenter la liaison Application ↔ BusinessCapability.
      La table app_capability_map et la relation Prisma existent déjà.
      À faire :
      - capabilityIds?: string[] dans CreateApplicationDto + UpdateApplicationDto
      - validateForeignKeys() : vérifier les IDs capabilities (pattern itComponents)
      - findOne() + findAll() : inclure capabilities dans Prisma include
      - mapApplicationResponse() : mapper businessCapabilities dans la réponse
      - create() + update() : sync junction app_capability_map (delete+recreate, pattern itComponents)
      ✅ Code + OpenAPI + tests Supertest/Playwright complétés pour le support BC.
      ✅ Validation exécutée dans le container backend : `npm run build` OK + `npm run test:e2e -- --runInBand test/FS-06-applications.e2e-spec.ts` OK (21/21).
      Gate T-026 (front) : ce back amendment doit être done avant.

  - id: T-026
    nom: "Amendment FS-06-FRONT — Sélecteur Business Capabilities dans Applications"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-10"
    sprint: S3
    theme: FS-07
    type: impl
    feature: FS-06-FRONT
    sessions:
      - tool: CL
        id: "c4d8e2"
        nom: "impl capabilityIds selector FS-06-FRONT"
    notes: |
      Amendment FS-06-FRONT : liaison Application ↔ BusinessCapability exposée côté UI.

      Livraison (2026-04-10) :
      - types/application.ts : BusinessCapabilityMapping, businessCapabilities[] sur Application/ListItem, capabilityIds dans ApplicationFormValues
      - ApplicationForm.tsx : section BC avec Dialog multi-select (pattern itComponents, sans rôle), prop availableOptions.businessCapabilities
      - ApplicationNewPage + ApplicationEditPage : useBusinessCapabilities({ limit: 200 }), capabilityIds initial/init depuis BC existantes
      - ApplicationDetailPage : chips cliquables vers /business-capabilities/:id dans section Relations
      - fr.json : 7 clés applications.form.businessCapabilitiesLabel + relations.businessCapabilities*

      Build frontend : 0 erreurs TypeScript, 0 erreurs Vite.
      Note : BusinessCapabilityEditPage.tsx:296 string hardcodée non adressée (hors scope T-026).

  - id: T-027
    nom: "Commande make db-reset-reseed + seed-all.ts orchestrateur"
    statut: done
    assigned_agent: data
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "F-999"
    type: impl
    feature: "SEED"
    sessions:
      - tool: CL
        id: "f3a912"
        nom: "make db-reset-reseed + seed-all.ts + fix app_provider_map"
    notes: |
      Makefile : cible db-reset-reseed — TRUNCATE CASCADE toutes tables métier
      (entity_tags, app_capability_map, app_data_object_map, app_it_component_map,
      app_provider_map, interfaces, applications, business_capabilities, data_objects,
      it_components, providers, domains, tag_values, tag_dimensions, audit_trail)
      puis relance seed-all.ts.
      Tables préservées : users, roles, permissions, role_permissions, refresh_tokens.
      backend/prisma/seed-all.ts : orchestrateur 6 seeds dans l'ordre.
      backend/package.json : prisma.seed → seed-all.ts (était seed.ts seul).
      Vérification : 10 domains + 25 applications + token OK.

  - id: T-028
    nom: "Fix GET /domains — réponse non paginée (page /domains vide)"
    statut: done
    assigned_agent: back
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "F-999"
    type: bug
    feature: "DOMAINS"
    sessions:
      - tool: CL
        id: "f3a912"
        nom: "fix domains.service.ts findAll pagination"
    notes: |
      Page /domains vide malgré 10 domaines en base.
      Root cause : DomainsService.findAll() retournait Domain[] brut,
      le frontend attend { data: Domain[], meta: { page, limit, total, totalPages } }.
      Fix : QueryDomainsDto créé, findAll() mis à jour avec count+skip+take+meta,
      controller @Query() ajouté.
      Fichiers : dto/query-domains.dto.ts (créé), domains.service.ts, domains.controller.ts.

  - id: T-029
    nom: "Uniformiser le menu d'actions des listes frontend"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: "docs/05-Project/20260408/SESSION-HANDOFF-front-list-actions-menu.md"
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-05"
    type: impl
    feature: "FS-05-FRONT"
    sessions:
      - tool: OC
        id: "2b0571"
        nom: "uniformize list row actions menu and add shared component"
    notes: |
      Refactor des listes Applications, Business Capabilities, Composants IT, Fournisseurs,
      Domaines et Data Objects pour remplacer les icônes d'action inline (Edit/Delete)
      par un menu à 3 points réutilisable.

      Actions du menu : Voir détails, Modifier, Supprimer.
      Point de sortie commun : création du composant partagé `RowActionsMenu`.

      Fichiers modifiés :
      - frontend/src/components/shared/RowActionsMenu.tsx
      - frontend/src/components/shared/index.ts
      - frontend/src/i18n/locales/fr.json
      - frontend/src/pages/applications/ApplicationsListPage.tsx
      - frontend/src/pages/business-capabilities/BusinessCapabilitiesPage.tsx
      - frontend/src/pages/it-components/ITComponentListPage.tsx
      - frontend/src/pages/providers/ProvidersListPage.tsx
      - frontend/src/pages/domains/DomainsListPage.tsx
      - frontend/src/pages/data-objects/DataObjectListPage.tsx

      Validation : `npm run build` (frontend) validé.
      Commit de clôture : 2b0571b.

  - id: T-030
    nom: "Recette FS-07-FRONT — correctifs conformité checklist §12"
    statut: done
    assigned_agent: front
    session_active: ~
    date_resolution: "2026-04-08"
    sprint: S3
    theme: "FS-07"
    type: bug
    feature: "FS-07-FRONT"
    sessions:
      - tool: OC
        id: "f8a3d1"
        nom: "recette QA + hotfix conformité FS-07"
    notes: |
      Défauts détectés pendant T-024 (recette checklist §12) et corrigés immédiatement :
      - Expand/collapse liste arborescente : enfants non masqués (filtre hiérarchique inactif)
      - Tri criticality : valeurs nulles placées en premier (au lieu de nulls last)
      - Strings en dur dans composants/pages FS-07 (i18n non conforme)

      Correctifs appliqués :
      - flattenTree() enrichi avec parentId/parent pour supporter le filtrage hiérarchique
      - BusinessCapabilitiesPage : masquage réel des descendants repliés + tri criticality nulls-last
      - Remplacement des textes hardcodés par clés i18n dans Drawer/Matrix/Detail/Edit/List
      - fr.json : ajout des clés businessCapabilities.common.*, matrix.*, drawer.close/moreApplications,
        detail.noDomain/rootCapability/subtitle, form.relatedApplicationsCount/applicationsRelationHint

      Vérification : `rtk npm run build` (frontend) OK.

  - id: T-032
    nom: "Audit @Query() sans DTO — vérifier coercition string→number dans tous les controllers NestJS"
    statut: open
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: S3
    theme: "QA"
    type: debt
    feature: "BACK-AUDIT"
    sessions:
      - tool: CL
        id: "f26390"
        nom: "création tâche d'audit suite bug T-031"
    notes: |
      Suite au bug T-031 (page/limit reçus comme strings via @Query() sans DTO).
      Passer en revue tous les controllers NestJS qui utilisent @Query() avec un type plain object
      (pas une classe DTO avec @Transform / @Type(() => Number)).
      Vérifier en particulier tous les endpoints de pagination (skip/take Prisma).
      Correctif attendu : remplacer les plain types par des DTOs avec class-transformer,
      ou ajouter Number() dans les services concernés.

  - id: T-031
    nom: "Bug — Onglet Applications vide sur détail IT Component (liaisons non affichées)"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-09"
    sprint: S3
    theme: "FS-05"
    type: bug
    feature: "QA-IT-COMPONENTS"
    sessions:
      - tool: CL
        id: "f26390"
        nom: "investigation + fix backend + fixes frontend Drawer/DetailPage"
    notes: |
      Root cause : GET /it-components/:id/applications reçoit page/limit comme strings via @Query()
      (sans DTO NestJS), mais Prisma exige des integers pour skip/take.
      Fix : backend/src/it-components/it-components.service.ts — Number(query.page) || 1 / Number(query.limit) || 20.
      Frontend : ajout CircularProgress + reset appsPage dans Drawer (useEffect on itComponent?.id).

  - id: T-033
    nom: "Business capabilities FIX : retirer la vue tree"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-11"
    sprint: S3
    theme: "FS-07"
    type: debt
    feature: "FS-07-FRONT"
    sessions:
      - tool: CL
        id: "buzzing"
        nom: "suppression BusinessCapabilityTree + toggle 2 vues + tests Playwright"
    notes: |
      Retirer la vue tree des Business Capabilities.
      Évaluer d'abord : specs (FS-07-FRONT) + composants front (BusinessCapabilityTree) + couverture de tests.
      Résolution 2026-04-11 : BusinessCapabilityTree.tsx supprimé, import AccountTreeIcon retiré,
      ToggleButton tree retiré, bloc render tree retiré, ViewMode réduit à 'list'|'matrix',
      clé i18n views.tree supprimée, tests Playwright mis à jour (2 vues, bascule liste/matrix).

  - id: T-034
    nom: "Business capabilities EVOL : vue matrix — chip criticité"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-11"
    sprint: S3
    theme: "FS-07"
    type: impl
    feature: "FS-07-FRONT"
    sessions:
      - tool: CL
        id: "buzzing"
        nom: "BusinessCapabilityMatrix — fond neutre + CriticalityChip + suppression badge niveau"
    notes: |
      Remplacer la couleur de background par un CriticalityChip dans la vue matrix.
      Supprimer la mention de niveau L0/L1/L2, afficher la criticité à la place.
      Objectif : alléger l'affichage visuel trop chargé avec les couleurs de fond actuelles.
      Résolution 2026-04-11 : getCriticalityColor + useTheme retirés, backgroundColor supprimé,
      badge L{level} supprimé, CriticalityChip ajouté dans le corps de la tuile.

  - id: T-035
    nom: "Business capabilities EVOL : vue matrix — limiter à 2 domaines L0 en desktop wide"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-11"
    sprint: S3
    theme: "FS-07"
    type: impl
    feature: "FS-07-FRONT"
    sessions:
      - tool: CL
        id: "buzzing"
        nom: "BusinessCapabilityMatrix — md={6} unifié (2 colonnes L0 en desktop)"
    notes: |
      En format desktop wide, limiter l'affichage à 2 domaines L0 maximum par rangée (actuellement 3).
      Améliore la lisibilité sur écran large.
      Résolution 2026-04-11 : md={depth===0?4:6} → md={6} unifié dans renderTile.

  - id: T-037
    nom: "Spec FS-07-FRONT v1.1 — cohérence post T-033/T-034/T-035"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-11"
    sprint: S3
    theme: "FS-07"
    type: doc
    feature: "FS-07-FRONT"
    sessions:
      - tool: CL
        id: "buzzing"
        nom: "mise à jour spec v1.1 : suppression vue tree, matrix chip, 2 vues"
    notes: |
      Mise en cohérence de docs/03-Features-Spec/FS-07-Business-Capabilities-front.md
      après implémentation T-033/T-034/T-035.
      Corrections : titre 3→2 vues, §4.2 marquée supprimée, §4.3 matrix CriticalityChip,
      §5 fichiers BusinessCapabilityTree retiré, §6 i18n views.tree retiré,
      §9 gate MUI TreeView retirée, §10/§12 tests mis à jour, §11 commande OpenCode nettoyée.

  - id: T-036
    nom: "Polishing UI — Uniformisation cadre blanc pages détail (BusinessCapability, DataObject, ITComponent)"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-11"
    sprint: S3
    theme: "POLISH"
    type: impl
    feature: ~
    sessions:
      - tool: CL
        id: "a3f7e1"
        nom: "Paper elevation=0 + cadre blanc sur 3 pages détail + AppBreadcrumbs/PageHeader ITComponent"
    notes: |
      Aligner BusinessCapabilityDetailPage, DataObjectDetailPage et ITComponentDetailPage
      sur le rendu "cadre blanc" de ApplicationDetailPage
      (référence visuelle : /applications/11a1cd07-6eaf-4951-9847-15ef884ef35d).
      Checklist :
      - [x] BusinessCapabilityDetailPage — Stack → Paper elevation=0 border divider, bouton retour sorti du Paper
      - [x] DataObjectDetailPage — Tabs+contenu → Paper elevation=0 border divider, px=2 sur tabs, p=4 sur info
      - [x] ITComponentDetailPage — Breadcrumbs inline → AppBreadcrumbs, titre Box → PageHeader, Tabs+contenu → Paper elevation=0
      Build frontend : 0 erreurs TypeScript, 0 erreurs Vite.

  - id: T-037
    nom: "IT Components : Vue liste — Changement affichage nombre apps en Chip MUI"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-12"
    sprint: S3
    theme: "FS-05"
    type: impl
    feature: "FS-05-FRONT"
    sessions:
      - tool: OC
        id: "k2-5front"
        nom: "uniformisation Chips compteurs vues liste (IT/Providers/DataObjects)"
    notes: |
      Uniformisation des compteurs d'applications liées en Chip MUI outlined+info.
      
      Fichiers modifiés :
      - ITComponentListPage.tsx : Chip outlined+info, conditionnel si > 0
      - ProvidersListPage.tsx : idem pour appProviderMaps
      - DataObjectListPage.tsx : idem pour appDataObjectMaps
      
      Pattern établi : Chip size=small variant=outlined color=info,
      cellule vide si valeur = 0 pour alléger l'affichage.
      Commit : 7d72a00

  - id: T-038
    nom: "Business Capabilities : Vue liste — Évolution UX du tri + indicateurs visuels de profondeur"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-12"
    sprint: S3
    theme: "FS-07"
    type: impl
    feature: "FS-07-FRONT"
    sessions:
      - tool: CL
        id: "f9d3b1"
        nom: "tri hiérarchique + visuels depth + expand/collapse all"
    notes: |
      Le pattern UX standard (entêtes de colonnes cliquables ASC/DESC) s'applique mal
      à la liste arborescente des Business Capabilities : les nœuds enfants "suivent"
      leur parent, ce qui brise l'ordre global attendu par l'utilisateur.

      Résolution 2026-04-12 :
      - sortTreeHierarchically() dans businessCapability.utils.ts : tri récursif par siblings
      - TableSortLabel supprimés ; Select "Trier par" (Nom A→Z/Z→A, Criticité ↑/↓) dans toolbar
      - Indicateurs visuels de profondeur : gras sur L0, FiberManualRecord (●) sur feuilles,
        chips niveau L0/L1/L2/L3 en dégradé gris, couleur texte dégradée (#424242→#BDBDBD)
      - underline="hover" sur les liens de noms
      - Bouton expand/collapse all (IconButton + Tooltip, icône UnfoldMore/UnfoldLess) dans toolbar
      Bug résiduel → T-041 (recherche libre ne fonctionne plus).

  - id: T-039
    nom: "Amendment FS-06-BACK — Contrôle dépendances Interfaces avant suppression Application"
    statut: open
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: S4
    theme: "FS-08"
    type: impl
    feature: "FS-06-BACK"
    sessions:
      - tool: CL
        id: "fs08sp"
        nom: "spec FS-08-BACK v1.1 — identification dette suppression Application"
      - tool: OC
        id: "a9f3e1"
        nom: "prérequis T-039 validés après T-040"
    notes: |
      Identifié lors de la rédaction FS-08-BACK (2026-04-12) — F-999 Item 24.
      ApplicationsService.remove() ne vérifie pas _count.sourceInterfaces ni
      _count.targetInterfaces avant suppression. La FK ON DELETE NO ACTION bloque
      en base mais ne retourne pas de DEPENDENCY_CONFLICT propre au client.

      ✅ Prérequis levés : FS-08-BACK done (T-040, 2026-04-12).
      Relations Prisma sourceInterfaces/targetInterfaces disponibles sur model Application.

      À faire :
      - Ajouter sourceInterfaces + targetInterfaces dans le select _count de remove()
      - Lever ConflictException DEPENDENCY_CONFLICT si total > 0
      - Ajouter test Supertest dans FS-06-applications.e2e-spec.ts :
        créer interface liée, tenter DELETE app → 409 DEPENDENCY_CONFLICT

  - id: T-040
    nom: "Implémentation FS-08-BACK — Module Interfaces (NestJS + Prisma + tests)"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: "docs/05-Project/20260412/SESSION-HANDOFF-back-t040-fs08.md"
    date_resolution: "2026-04-12"
    sprint: S4
    theme: "FS-08"
    type: impl
    feature: "FS-08-BACK"
    sessions:
      - tool: CL
        id: "fs08sp"
        nom: "rédaction spec FS-08-BACK v1.1 — PRD Interfaces backend"
      - tool: OC
        id: "a9f3e1"
        nom: "impl FS-08-BACK module NestJS + tests"
    notes: |
      Spec stabilisée : docs/03-Features-Spec/FS-08-Interfaces-back.md (v1.1)
      Débloque : T-006 (tests Playwright applications-dependencies), T-039 (amendment FS-06-BACK)

      Périmètre :
      - Migration schéma (enums InterfaceType/InterfaceFrequency, fix id/updatedAt,
        drop tags[], drop latency_ms, technical_contact texte libre, add description/comment)
      - Module NestJS : interfaces.module/controller/service + DTOs create/update/query
      - Tests Jest unit (13 cas) + Supertest e2e (18 cas)
      - Seed 2 interfaces de démo (dépend des apps seedées FS-06)
      - openapi.yaml mis à jour (NFR-GOV-001)

  - id: T-042
    nom: "Amendment FS-08-BACK — champ middlewareAppId (composant de médiation)"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-12"
    sprint: S4
    theme: "FS-08"
    type: impl
    feature: "FS-08-BACK"
    sessions:
      - tool: OC
        id: "c7d3e9"
        nom: "amendment backend middlewareAppId"
    notes: |
      ✅ Amendment FS-08-BACK complété — champ middlewareAppId (composant de médiation optionnel).
      
      Livraison 2026-04-12 :
      - Prisma : middlewareAppId + relation InterfaceMiddlewareApp + index idx_interfaces_middleware
      - Model Application : middlewareInterfaces[] relation inverse
      - Migration : ALTER TABLE interfaces ADD COLUMN middleware_app_id UUID
      - DTOs : middlewareAppId dans CreateInterfaceDto + UpdateInterfaceDto (PartialType) + QueryInterfaceDto
      - Service : validateApplications() étendue avec middleware, include middlewareApp dans findAll/findOne/create/update
      - Service update() : support unset explicite middlewareAppId=null
      - openapi.yaml : query param middlewareAppId + champs middlewareAppId/middlewareApp dans InterfaceResponse + InterfaceListItem + Create/Update DTOs
      - Tests unitaires : 20/20 pass (15 existants + 5 nouveaux : filtre middlewareAppId, create avec middleware, update set/unset, 404 middleware)
      - Tests e2e : 25 cas définis (bloqués par contexte auth en test — validé manuellement)
      - Seed : Interface "Portail → API Gateway" avec middleware (apps[3]) + SQL middleware_app_id
      
      Validation manuelle OK :
      - GET /api/v1/interfaces retourne middlewareApp + middlewareAppId
      - GET /api/v1/interfaces?middlewareAppId=<uuid> filtre correctement (1 résultat)
      
      Gate T-043 (FS-08-FRONT) débloquée — prêt pour implémentation frontend.

  - id: T-043
    nom: "Spec FS-08-FRONT — Interfaces frontend"
    statut: done
    assigned_agent: spec
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-12"
    sprint: S4
    theme: "FS-08"
    type: spec
    feature: "FS-08-FRONT"
    sessions:
      - tool: OC
        id: "curr-ses"
        nom: "rédaction spec FS-08-FRONT v0.1"
    notes: |
      Spec frontend Interfaces v0.1 rédigée (docs/03-Features-Spec/FS-08-Interfaces-front.md).
      Pattern PNS-02 drawer read-only, 4 pages (List/Detail/New/Edit), filtres serveur.
      Colonnes liste : Source | Middleware | Cible | Type (StatusChip FR) | Criticité (CriticalityChip) | Fréquence.
      Filtres : sourceApp + middlewareApp + targetApp + type + criticality.
      RM-IF-01 : validation client-side sourceApp ≠ targetApp avant soumission.
      Suppression libre (pas de DEPENDENCY_CONFLICT).
      Enums avec labels FR via i18n (interfaces.type.*, interfaces.frequency.*).
      Dépend de T-042 (amendment middlewareAppId) pour passer à stable.
      FS-08-Interfaces-back.md mis à jour statut → done.

  - id: T-041
    nom: "Bug — BC vue liste : la recherche libre ne fonctionne plus"
    statut: in_progress
    assigned_agent: front
    session_active: "f4a8c2"
    handoff_ref: ~
    date_resolution: ~
    sprint: S3
    theme: "FS-07"
    type: bug
    feature: "FS-07-FRONT"
    sessions: []
    notes: |
      Signalé après les évolutions T-038 (tri hiérarchique) et améliorations visuelles.
      La recherche dans la barre de filtres de la vue liste BC semble ne plus déclencher
      de filtrage. À investiguer : vérifier le flux searchInput → searchValue (handleSearchSubmit),
      l'impact du useMemo flatList sur la dépendance treeData, et si le useEffect
      d'initialisation de expanded interfère avec les résultats filtrés.

  - id: T-044
    nom: "Implémentation FS-08-FRONT — Module Interfaces (React + MUI)"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-04-12"
    sprint: S4
    theme: FS-08
    type: impl
    feature: FS-08-FRONT
    sessions:
      - tool: OC
        id: "a3f8e1"
        nom: "impl FS-08-FRONT module Interfaces"
    notes: |
      Toutes les gates de la spec FS-08-Interfaces-front.md §9 sont levées :
      - T-040 (FS-08-BACK) ✅ done 2026-04-12
      - T-042 (amendment middlewareAppId) ✅ done 2026-04-12
      - T-043 (spec FS-08-FRONT) ✅ done 2026-04-12

      Prérequis manuels avant session OpenCode :
      1. Ajouter clés interfaces.* dans src/i18n/locales/fr.json (cf. spec §6)
      2. Câbler routes dans App.tsx (cf. spec §8)
      3. Ajouter entrée Sidebar Interfaces dans AppShell (CompareArrowsIcon)
      4. Passer FS-08-Interfaces-front.md statut → stable

      Périmètre :
      - InterfaceListPage + InterfaceDrawer (PNS-02) + filtres serveur
      - InterfaceDetailPage, InterfaceNewPage, InterfaceEditPage
      - InterfaceForm avec RM-IF-01 (sourceApp ≠ targetApp)
      - Hooks React Query (useInterfaces, useInterface, CRUD mutations)
      - types/interface.ts + utils/interfaces.utils.ts (formatInterfaceTitle)
      - CriticalityChip réutilisé depuis @/components/business-capabilities/

      Tests Playwright UI délégués à l'agent qa (cf. spec §10).

  - id: T-045
    nom: "Bug — Erreur 500 sur PATCH /interfaces/:id lors de l'enregistrement"
    statut: open
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: S4
    theme: FS-08
    type: bug
    feature: FS-08-BACK
    sessions: []
    notes: |
      Erreur 500 Internal Server Error sur PATCH /api/v1/interfaces/:id
      lors de l'enregistrement en édition/création d'interface.
      Log réseau : interfaces.ts:60 PATCH http://localhost:5173/api/v1/interfaces/{uuid} → 500.
      À investiguer : stack trace backend, validation DTO, transaction Prisma.
