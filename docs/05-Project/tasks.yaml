tasks:
  - id: T-003
    nom: "Implémenter FS-09 Dependency Graph — frontend ReactFlow"
    statut: in_progress
    assigned_agent: front
    session_active: "adcda9"
    date_resolution: ~
    sprint: S2
    theme: "FS-09"
    type: impl
    feature: "FS-09-FRONT"
    priorité: medium
    sessions:
      - tool: OC
        id: "a1b2c3"
        nom: "rituel ouverture - gate bloqué : spec vide"
    notes: |
      Gates levées (2026-05-03) :
        - FS-09-BACK impl done : T-087 done (module backend/src/graph/, G-01 à G-09 validées)
        - FS-09-FRONT spec stable : T-085 done (draft, suffisant pour démarrer)
      Gate résiduelle : T-086 (BC recursive WITH RECURSIVE) — US-04 partiel acceptable pour P1.
      Handoff back : docs/05-Project/20260503/SESSION-HANDOFF-back-t087.md
      Spec front : docs/03-Features-Spec/FS-09-Dependency-Graph/FS-09-Dependency-Graph-front.md

  - id: T-009
    nom: "Migration tests Cypress → Playwright — Data Objects (F-999 Item 22)"
    statut: open
    assigned_agent: qa
    session_active: ~
    date_resolution: ~
    sprint: S3
    theme: "F-999"
    type: impl
    feature: "QA-DATA-OBJECTS"
    priorité: medium
    sessions: []
    notes: |
      F-999 Item 22 — 37 tests Cypress (frontend/cypress/e2e/data-objects.cy.ts) à migrer.
      Destination : e2e/tests/data-objects/data-objects.spec.ts (Playwright).
      Cypress inutilisable sur cet env (dépendances libnspr4/X11 manquantes).
      Après migration : supprimer frontend/cypress/ et retirer dépendances Cypress de package.json.

  - id: T-012
    nom: "Débloquer sélecteur Owner — API Users mock (F-999 Item 12b)"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: "20260502/SESSION-HANDOFF-back-t012.md"
    date_resolution: 2026-05-03
    sprint: S3
    theme: "F-999"
    type: bug
    feature: "FS-06-FRONT"
    priorité: medium
    sessions:
      - tool: OC
        id: "dac536"
        nom: "implémentation filtre users isActive"
    notes: |
       2026-05-03 — F-999 Item 12b résolu côté back+front :
       - GET /api/v1/users?isActive=true implémenté côté back.
       - Nouveau hook `useUsers()` créé dans `frontend/src/api/users.ts`.
       - `MOCK_USERS` supprimé au profit de l'appel API dans
         `ApplicationNewPage.tsx` et `ApplicationEditPage.tsx`.
       - Fallback ajouté en édition : conservation du owner courant même si inactif.
       - Gate validée : sélecteur Owner peuplé sur création/édition.

  - id: T-049
    nom: "Spec — Mettre au clair la liaison entre Application et Data Objects"
    statut: open
    assigned_agent: spec
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: S4
    theme: "FS-05"
    type: spec
    feature: "FS-05-BACK"
    priorité: low
    sessions: []

  - id: T-050
    nom: "QA — 3 tests API en échec non tracés (applications-validation, domains-crud, it-components)"
    statut: open
    assigned_agent: qa
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: S4
    theme: "QA"
    type: bug
    feature: "BACK-AUDIT"
    priorité: medium
    sessions:
      - tool: OC
        id: "a4f2e1"
        nom: "identifiés lors de la validation T-006 (2026-04-12)"
    notes: |
      3 tests Playwright en échec préexistants découverts lors de la session T-006 (2026-04-12).
      Score global : 100/107 après fix T-006 (6 échecs — 3 couverts par T-007, 3 non tracés ci-dessous).

      1. applications-validation.api.spec.ts:78
         POST /applications should return 404 for non-existent business capabilityId
         Reçu : 400 — attendu : 404
         Cause probable : validation capabilityId retourne 400 BAD_REQUEST au lieu de 404 NOT_FOUND.

      2. domains-crud.api.spec.ts:5
         GET /domains should return array of domains
         Échec intermittent ou structure de réponse inattendue — à investiguer.

      3. it-components-crud.api.spec.ts:72
         DELETE /it-components/:id - should delete IT component without dependencies
          Échec au DELETE — possible 409 DEPENDENCY_CONFLICT résiduel ou app liée non nettoyée.

  - id: T-051
    nom: "Interfaces — Décision : comment traiter les interfaces manuelles (model / back / front)"
    statut: open
    assigned_agent: arch
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: S4
    theme: "FS-08"
    type: decision
    feature: "FS-08"
    priorité: medium
    sessions: []
    notes: |
      Question transverse : comment modéliser et traiter les interfaces créées/saisies manuellement
      (par opposition aux interfaces découvertes automatiquement).
      Impacte le modèle Prisma, le backend NestJS et le frontend React.
      Décision structurante à formaliser avant impl ou spec FS-08.

  - id: T-062
    nom: "Backend — Omnisearch filtres avancés + pagination offset (T-060-bis)"
    statut: open
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: S4
    theme: "FS-11"
    type: impl
    feature: "FS-11-BACK-P2"
    priorité: low
    sessions: []
    notes: |
      Scope P2 (ex-T-060-bis) : ajouter filtres domainIds[], tagIds[], pagination offset.
      Dépend de : T-060 done.
      Amendement US : domainIds[], tagIds[], page, limit max 100, meta totalPages/currentPage.
      Spec : docs/03-Features-Spec/FS-11-omnisearch-userstories.md §Amendement A.

  - id: T-069
    nom: "Déporter rapports Playwright e2e/reports/ → reports/playwright/ avec horodatage"
    statut: open
    assigned_agent: qa
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: S4
    theme: "QA"
    type: impl
    feature: "QA-REPORTS"
    priorité: medium
    sessions: []
    notes: |
      Suite T-067 : uniformiser l'horodatage des rapports de tests.
      Scope : déporter les rapports Playwright de e2e/reports/ vers reports/playwright/.
      Modifications prévues :
      - e2e/playwright.config.ts : outputFolder 'reports/html' → '../../../reports/playwright/YYYYMMDD-HHMMSS'
      - e2e/package.json : script "test:report" → adapter le chemin
      - .gitignore : ajouter reports/playwright/ (ou ajuster le pattern existant)
      - Optionnel : Makefile target 'test-e2e-report' pour ouvrir le rapport le plus récent
      Dépend de : T-067 done (format d'horodatage validé).
      Non bloquant : peut démarrer dès maintenant.

  - id: T-079
    nom: "Back — Sécurité : ajouter @RequirePermissions sur les endpoints write de tags"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: 2026-05-16
    sprint: S4
    theme: "F-03"
    type: bug
    feature: "F-03-TAGS"
    priorité: high
    sessions:
      - tool: OC
        id: "e4f2a9"
        nom: "fix @RequirePermissions tags:write x3 + tags:read x2 — build PASS"
    notes: |
      Vulnérabilité de sécurité confirmée (revue 2026-04-29, confiance 9/10).
      Le PermissionsGuard retourne true si @RequirePermissions est absent.
      Tout utilisateur authentifié (rôle read-only inclus) peut :
        - POST /tags/resolve → créer des valeurs de tag
        - PUT /tags/entity/:type/:id → remplacer les tags d'une entité
        - PUT /tags/entity/:type/:id/batch → écraser tous les tags d'une entité
      Fix : ajouter @RequirePermissions('tags:write') sur les 3 méthodes write.
      Défense : ajouter @RequirePermissions('tags:read') sur autocomplete + getEntityTags.
      Fichier : backend/src/tags/tags.controller.ts
      Référence spec : F-999 Item 25.

  - id: T-078
    nom: "QA — Tests Playwright pour les endpoints /users, /roles, /permissions"
    statut: open
    assigned_agent: qa
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: S4
    theme: "QA"
    type: test
    feature: "QA-RBAC"
    priorité: medium
    sessions: []
    notes: |
      Suite T-066 : les endpoints /users, /roles, /permissions sont désormais documentés
      dans openapi.yaml v1.6.0. Ils n'ont pas de couverture Playwright.
      Scope :
      - e2e/tests/users/ (CRUD + soft delete isActive=false)
      - e2e/tests/roles/ (CRUD + PUT /roles/:id/permissions)
      - e2e/tests/permissions/ (GET list + POST)
      Pattern à suivre : e2e/tests/domains-crud.api.spec.ts (structure auth + cleanup).
      Gate RBAC : tester avec un token admin (permission users:read/write, roles:read/write).

  - id: T-074
    nom: "Réévaluer la gestion des tags (fonctionnel, interface, clé, valeur etc...)"
    statut: open
    assigned_agent: spec
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: S4
    theme: "F-03"
    type: review
    feature: "F-03-TAGS"
    priorité: medium
    sessions: []
    notes: |
      Commencer impérativement par ré-étudier les spécifications PRD des tags
      (docs/03-Features-Spec/F-foundations/F03-Dimension-Tag-Foundation.md +
      docs/01-Product/ARK-Product-Brief.md) avant toute vérification de l'implémentation.
      Périmètre : types de tags (fonctionnel, interface, clé, valeur…), modèle Prisma,
      usages actuels dans les entités (Applications, IT Components, Data Objects, Interfaces…).
      Objectif : identifier les écarts spec/impl et proposer une réévaluation cohérente.

  - id: T-075
    nom: "Frontend — Moderniser l'affichage des pages NEW/EDIT pour Applications"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: 2026-04-29
    sprint: S4
    theme: "FS-06"
    type: impl
    feature: "FS-06-FRONT"
    priorité: medium
    sessions:
      - tool: OC
        id: "a7f3c2"
        nom: "modernisation ApplicationNewPage + ApplicationEditPage + ApplicationForm — pattern MUI v9 post-T-064"
    notes: |
      Appliquer le pattern de design modernisé (post-T-064 MUI v9) aux pages
      ApplicationNewPage et ApplicationEditPage.
      Cohérence visuelle avec ApplicationDetailPage (T-065) et les patterns MUI v9 (T-064).

  - id: T-076
    nom: "Frontend — Évaluer et moderniser les pages NEW/EDIT des autres entités (Business Cap, Interfaces, Data Object, IT Components, Fournisseurs)"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: "docs/05-Project/20260429/SESSION-HANDOFF-front-t076.md"
    date_resolution: 2026-04-29
    sprint: S4
    theme: "F-999"
    type: review
    feature: "F-999-FRONT-FORMS"
    priorité: medium
    sessions:
      - tool: OC
        id: "k2e6a4"
        nom: "modernisation 8 wrappers NEW/EDIT — pattern MUI v9 post-T-075"
      - tool: OC
        id: "k2e6a4"
        nom: "review fixes + commit T-075/T-076"
    notes: |
      Suite T-075 : après modernisation des formulaires Applications,
      évaluer l'état des pages NEW/EDIT sur les autres entités :
      - BusinessCapabilityNew/Edit
      - InterfaceNew/Edit
      - DataObjectNew/Edit
      - ITComponentNew/Edit
      - ProviderNew/Edit (Fournisseurs)
      Identifier les écarts par rapport au pattern MUI v9 et planifier les corrections.
        Dépend de : T-075 done (pattern de référence établi).

  - id: T-077
    nom: "Frontend — Remplacer les Avatars de certaines entités par des icônes officielles"
    statut: open
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: S4
    theme: "F-999"
    type: impl
    feature: "F-999-FRONT-ICONS"
    priorité: medium
    sessions: []

  - id: T-080
    nom: "RBAC — Permissions fines sur la recherche et filtrages par domaine/rôle"
    statut: open
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: ~
    theme: "FS-01"
    type: impl
    feature: "FS-01-BACK"
    priorité: low
    sessions: []
    notes: |
      Contexte : SearchController est actuellement protégé par JWT uniquement
      (pas de permission spécifique). Une permission search:read (ou des filtres
      par domaine, par rôle utilisateur) sera à créer quand le modèle RBAC évoluera.
      Scope minimum :
      - Créer permission search:read + seeder
      - Ajouter @RequirePermissions('search:read') sur SearchController
      - Éventuellement : filtrer les résultats selon les domaines accessibles au rôle

  - id: T-081
    nom: "Back — Corriger no-control-regex dans tags.service.ts (détecté MegaLinter)"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: 2026-04-29
    sprint: S4
    theme: "QA"
    type: bug
    feature: "F-03-TAGS"
    priorité: high
    sessions:
      - tool: CL
        id: "a3f7c9"
        nom: "détecté par ESLint (MegaLinter) lors de la session qualité"
      - tool: OC
        id: "553729"
        nom: "fix no-control-regex + 65 no-unused-vars + dead code — commit 553729f"
    notes: |
      ESLint rule no-control-regex — backend/src/tags/tags.service.ts:15
      Regex /[\\"'\x00-\x1F<>]/ remplacée par hasInvalidPathChars() via charCodeAt().
      65 warnings no-unused-vars corrigés sur 36 fichiers (backend, e2e, frontend).
       Appel mort getAncestorPaths() retiré de resolveOrCreate() (méthode conservée — API publique F-03).

  - id: T-082
    nom: "Interface list — nom manquant + filtre libre sur le nom"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: 2026-05-03
    sprint: S4
    theme: "FS-08"
    type: bug
    feature: "FS-08-FRONT"
    priorité: medium
    sessions:
      - tool: OC
        id: "0db803"
        nom: "clôture T-082 + test e2e minimal liste interfaces"
    notes: |
      La liste des Interfaces (InterfaceList) n'affiche pas le nom de l'interface.
      Le nom doit être le premier champ affiché.
      Ajouter un filtre de recherche libre sur le nom.

  - id: T-083
    nom: "Back — Suppression application impossible (DELETE /applications/:id → 500)"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: 2026-05-02
    sprint: S4
    theme: "FS-06"
    type: bug
    feature: "FS-06-BACK"
    priorité: high
    sessions:
      - tool: OC
        id: "a4c2d1"
        nom: "fix middlewareInterfaces dependency check + remove dead P2003 catch"
    notes: |
      Impossible de supprimer une application même sans liaisons.
      Exemple : applications.ts:124
        DELETE http://localhost:5173/api/v1/applications/1dab9ce7-bc55-4f34-8a61-129ee6be8e4a
        500 (Internal Server Error)
      
      Cause : getDependencies() oubliait de compter middlewareInterfaces.
      Fix : ajouter middlewareInterfaces au _count.select.
      Cleanup : retirer le try/catch P2003 dead code (pre-check suffit).
      Tests : 21/21 PASS (FS-06 e2e).

  - id: T-084
    nom: "Data — Remettre à jour schema.sql et générer un diagramme Mermaid propre"
    statut: done
    assigned_agent: data
    session_active: ~
    handoff_ref: ~
    date_resolution: 2026-05-02
    sprint: S4
    theme: "QA"
    type: doc
    feature: "DATA-SCHEMA"
    priorité: medium
    sessions:
      - tool: CL
        id: "a8f3c1"
        nom: "schema.sql v1.0 + docs/04-Tech/README.md avec diagramme Mermaid"

  - id: T-085
    nom: "Spec — FS-09 Dependency Graph (back + front)"
    statut: done
    assigned_agent: spec
    session_active: ~
    handoff_ref: "docs/05-Project/20260503/SESSION-HANDOFF-spec-t085.md"
    date_resolution: 2026-05-03
    sprint: S4
    theme: "FS-09"
    type: spec
    feature: "FS-09-BACK"
    priorité: high
    sessions:
      - tool: CL
        id: "f3a9c2"
        nom: "spec FS-09-BACK + FS-09-FRONT rédigées (draft) — interview conception + 4 fichiers"
    notes: |
      Rédiger FS-09-BACK et FS-09-FRONT pour débloquer T-003.
      User stories : FS-09-Dependency-Graph-userstories.md (rédigées, P1/P2 split fait).
      API : GET /api/v1/graph?focalType=&focalId=&depth=&layers= (D7).
      Permission : applications:read. PNS-02 drawer existant. Dagre layout P1.
      US P2 : US-08, US-09, US-11, US-12. D12 GroupNode Domain = P2.
      Gate US-04 : T-086 (BC recursive WITH RECURSIVE) créée.

  - id: T-086
    nom: "Back — Gate FS-09 US-04 : traversal récursif BC (WITH RECURSIVE)"
    statut: open
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: S4
    theme: "FS-09"
    type: impl
    feature: "FS-09-BACK"
    priorité: medium
    sessions: []
    notes: |
      Prérequis US-04 (focal=business_capability avec expansion récursive BC enfants).
      Requête WITH RECURSIVE sur business_capabilities + join app_capability_map.
      Bloque l'implémentation focalType=bc complète dans GraphService (gate G-10 de FS-09-BACK).
      Sans cette tâche : focalType=bc retourne uniquement la BC focale + apps directes (partiel).

  - id: T-087
    nom: "Back — Implémenter FS-09 Dependency Graph (backend/src/graph/)"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: "docs/05-Project/20260503/SESSION-HANDOFF-back-t087.md"
    date_resolution: 2026-05-03
    sprint: S4
    theme: "FS-09"
    type: impl
    feature: "FS-09-BACK"
    priorité: high
    sessions:
      - tool: CL
        id: "fs09bk"
        nom: "implémentation complète module graph/ — BFS x6 focalTypes, 8/8 e2e PASS, G-01 à G-09 validées"
    notes: |
      Créer module backend/src/graph/ avec GET /api/v1/graph endpoint.
      BFS traversal focalType x6, depth 1-3, layers CSV.
      Permission : applications:read (réutilisation).
      Gates G-01 à G-09 validées. G-10 (BC récursif) bloquée par T-086.
      Spec : docs/03-Features-Spec/FS-09-Dependency-Graph/FS-09-Dependency-Graph-back.md

  - id: T-088
    nom: "FS-09-P2 — Finaliser les User Stories P2 du graphe de dépendances"
    statut: open
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: P2
    theme: "FS-09"
    type: impl
    feature: "FS-09-P2"
    priorité: medium
    sessions: []
    notes: |
      ID roadmap ciblé : docs/05-Project/roadmap.yaml (FS-09-P2).
      Couvre les User Stories P2 identifiées dans `FS-09-Dependency-Graph-userstories.md` :
      - US-08 — Navigation historique (← →)
      - US-09 — Changement d'algorithme de layout (ELK / Force-directed)
      - US-11 — Export Mermaid depuis l'état visible
      - US-12 — Deep linking complet + gestion focalId invalide
      - D12 — Domain GroupNode conteneur
      Dépend de `T-087` (module graph backend) et `T-085` (spec front/back).

  - id: T-089
    nom: "F-999-06 — Ajouter endpoint healthcheck"
    statut: open
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: P2
    theme: "F-999"
    type: impl
    feature: "F-999-06"
    priorité: low
    sessions: []
    notes: |
      ID roadmap ciblé : docs/05-Project/roadmap.yaml (F-999-06).
      Spécification source : `docs/03-Features-Spec/F-foundations/F99-Technical-Debt.md`, section Item 6.
      Implémentation attendue côté backend : `GET /api/v1/health` non authentifié, réponse `status: ok, db: ok`.
      Vérification préconisée : `GET /api/v1/health` → 200.

  - id: T-090
    nom: "F-999-07 — Appliquer politique CORS P2"
    statut: open
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: P2
    theme: "F-999"
    type: impl
    feature: "F-999-07"
    priorité: low
    sessions: []
    notes: |
      ID roadmap ciblé : docs/05-Project/roadmap.yaml (F-999-07).
      Spécification source : `docs/03-Features-Spec/F-foundations/F99-Technical-Debt.md`, section Item 7.
      Déployer une configuration CORS avec `CORS_ORIGIN` pour production on-premise.
      En dev local, comportement permissif (`*`) conservé.

  - id: T-091
    nom: "F-999-11 — Description Markdown des Applications"
    statut: open
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: P2
    theme: "F-999"
    type: impl
    feature: "F-999-11"
    priorité: medium
    sessions: []
    notes: |
      ID roadmap ciblé : docs/05-Project/roadmap.yaml (F-999-11).
      Spécification source : `docs/03-Features-Spec/F-foundations/F99-Technical-Debt.md`, section Item 11.
      Remplacer le champ description des Applications par rendu/édition Markdown simplifié.
      Cible P2 : conformité PNS-09 et réciprocité FS-06 front v1.1.

  - id: T-092
    nom: "F-999-16 — Customisation visuelle des rôles Provider"
    statut: open
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: P2
    theme: "F-999"
    type: impl
    feature: "F-999-16"
    priorité: medium
    sessions: []
    notes: |
      ID roadmap ciblé : docs/05-Project/roadmap.yaml (F-999-16).
      Spécification source : `docs/03-Features-Spec/F-foundations/F99-Technical-Debt.md`, section Item 16.
      Implémenter la personnalisation visuelle des rôles providers (palette dédiée, lisibilité, contraste).

  - id: T-093
    nom: "F-999-17 — Compléter les filtres dropdowns de ProvidersListPage"
    statut: open
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: P2
    theme: "F-999"
    type: impl
    feature: "F-999-17"
    priorité: medium
    sessions: []
    notes: |
      ID roadmap ciblé : docs/05-Project/roadmap.yaml (F-999-17).
      Spécification source : `docs/03-Features-Spec/F-foundations/F99-Technical-Debt.md`, section Item 17.
      Couvre les 2 filtres manquants : `contractType` et `expiryDate`.
      Le backend doit exposer les query params dans `QueryProvidersDto` + service.
      Le frontend doit activer les dropdowns et le filtrage via API.

  - id: T-094
    nom: "F-999-18 — Ajouter breadcrumbs sur pages Applications"
    statut: open
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: P2
    theme: "F-999"
    type: impl
    feature: "F-999-18"
    priorité: medium
    sessions: []
    notes: |
      ID roadmap ciblé : docs/05-Project/roadmap.yaml (F-999-18).
      Spécification source : `docs/03-Features-Spec/F-foundations/F99-Technical-Debt.md`, section Item 18.
      Ajouter la navigation PNS-11 sur :
      - ApplicationDetailPage
      - ApplicationNewPage
      - ApplicationEditPage

  - id: T-095
    nom: "F-999-19 — Ajouter breadcrumbs sur pages Domains"
    statut: open
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: P2
    theme: "F-999"
    type: impl
    feature: "F-999-19"
    priorité: medium
    sessions: []
    notes: |
      ID roadmap ciblé : docs/05-Project/roadmap.yaml (F-999-19).
      Spécification source : `docs/03-Features-Spec/F-foundations/F99-Technical-Debt.md`, section Item 19.
      Ajouter la navigation PNS-11 sur :
      - DomainDetailPage
      - DomainNewPage
      - DomainEditPage

  - id: T-096
    nom: "F-999-20 — Harmoniser breadcrumbs Providers"
    statut: open
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: P2
    theme: "F-999"
    type: impl
    feature: "F-999-20"
    priorité: medium
    sessions: []
    notes: |
      ID roadmap ciblé : docs/05-Project/roadmap.yaml (F-999-20).
      Spécification source : `docs/03-Features-Spec/F-foundations/F99-Technical-Debt.md`, section Item 20.
      Finaliser l'harmonisation des breadcrumbs Providers (Accueil + namespace i18n + spacing).

  - id: T-097
    nom: "F-999-21 — Créer composant partagé AppBreadcrumbs"
    statut: open
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: P2
    theme: "F-999"
    type: impl
    feature: "F-999-21"
    priorité: medium
    sessions: []
    notes: |
      ID roadmap ciblé : docs/05-Project/roadmap.yaml (F-999-21).
      Spécification source : `docs/03-Features-Spec/F-foundations/F99-Technical-Debt.md`, section Item 21.
      Créer `AppBreadcrumbs` dans `frontend/src/components/shared/` et refactorer les usages inline quand possible.
      Ajouter à `frontend/src/components/shared/index.ts`.

  - id: T-098
    nom: "Arch — Ajouter eslint-plugin-sonarjs (cognitive-complexity) au pipeline MegaLinter"
    statut: done
    assigned_agent: arch
    session_active: ~
    handoff_ref: "docs/05-Project/20260504/SESSION-HANDOFF-arch-t098.md"
    date_resolution: 2026-05-04
    sprint: S4
    theme: "QA"
    type: impl
    feature: "QA-ESLINT"
    priorité: medium
    sessions:
      - tool: OC
        id: "0c5f42"
        nom: "implémentation sonarjs backend + frontend, MegaLinter séparé, Makefile targets"
    notes: |
      Ajouter l'analyse de complexité cognitive au pipeline ESLint/MegaLinter.
      Scope :
      - Installer eslint-plugin-sonarjs (npm install --save-dev eslint-plugin-sonarjs)
      - Activer la règle sonarjs/cognitive-complexity (warn à 15)
      - Vérifier compatibilité avec la configuration MegaLinter existante (.eslintrc / eslint.config.*)
      Référence : MegaLinter supporte nativement les plugins ESLint via la config locale.

  - id: T-099
    nom: "Fix test ZAP — injections persistées en base Data Objects + cleanup"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: "docs/05-Project/20260504/SESSION-HANDOFF-back-t099.md"
    date_resolution: 2026-05-04
    sprint: S4
    theme: "QA"
    type: bug
    feature: "QA-DAST"
    priorité: high
    sessions:
      - tool: OC
        id: "a5c2f1"
        nom: "DTOs data-objects + whitelist:true + tests injection + SQL cleanup"
    notes: |
      Le scan OWASP ZAP a persisté des payloads de fuzzing dans la table data_objects
      (screenshot : docs/tmp/2026-05-04 215832_bugzap.png).
      Exemples visibles : "ZAP;cat /etc/passwd;", "ZAP|type %SYSTEMROOT%\win.ini",
      "http://www.google.com/search?q=ZAP", "]]>", etc.

      Cause : POST /api/v1/data-objects sans validation stricte + scan ZAP écrit en DB partagée.

      Fix côté back (prioritaire) :
      - Validation renforcée sur CreateDataObjectDto (name, description)
      - Rejeter les payloads de type command injection, XSS, ou URLs malformées

       Fix côté qa (secondaire) :
       - Isoler l'env DAST (DB dédiée ou mode passif ZAP)
       - Cleanup script post-scan pour retirer les entrées de test

  - id: T-100
    nom: "Audit global application — validation architecture + tech debt"
    statut: open
    assigned_agent: arch
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: S5
    theme: "QA"
    type: review
    feature: "QA-AUDIT"
    priorité: medium
    sessions:
      - tool: OC
        id: "0c5f42"
        nom: "création tâche + scope détaillé pour audit S5"
    notes: |
      Audit global ARK-EPM suite aux corrections S4 (T-098 ESLint, T-099 ZAP injection).
      Vérifier l'état d'ensemble de l'application et identifier la dette technique à tracker.

      Scope :
      1. Backend — structure + patterns
         - Conformité AGENTS.md (auth, RBAC, audit context, validation DTOs)
         - Modules NestJS : conventions d'import, gestion erreurs, logging
         - Services : utilisation PrismaService + setCurrentUser avant writes
         - Endpoints : auth guards (@Public si nécessaire), permissions (@RequirePermissions)

      2. Frontend — composants + état
         - Conformité MUI v9 exclusif (pas Tailwind, pas styled-components custom)
         - Components : hooks hooks patterns, React Query (useQuery/useMutation)
         - i18n : toutes strings via t('key') (scanner hardcoded strings)
         - Theme : vérifier DESIGN.md tokens appliqués

      3. Base de données — schema + migrations
         - Conformité Prisma : @@map snake_case, @default(dbgenerated()), @db.Uuid
         - Audit trail : trigger fn_audit_trigger() sur toutes tables
         - Indexes : sur foreign keys et colonnes WHERE/JOIN fréquentes

      4. Tests — couverture + stabilité
         - Playwright API : existence tests pour tous endpoints CRUD
         - Jest unit : couverture services critiques (auth, validation, business logic)
         - E2E UI : parcours critiques (login → create/edit/delete entités)
         - Flakiness : identifier tests intermittents

      5. Tech debt — F-999 items
         - Relire docs/03-Features-Spec/F-foundations/F99-Technical-Debt.md
         - Créer T-101..T-105 pour items P2 (sélection parmi 25 items)
         - Estimer effort, prioriser par ROI

       Livrables :
       - Tableau récapitulatif (conformité %)
       - Liste tâches nouvelles (T-101..T-105)
       - Rapport détails arch/tech debt pour review sprint S5

  - id: T-101
    nom: "Back — Harmoniser validation injection sur tous les DTOs (suite T-099)"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: 2026-05-05
    sprint: S4
    theme: "QA"
    type: debt
    feature: "QA-DAST"
    priorité: high
    sessions:
      - tool: OC
        id: "a5c2f1"
        nom: "audit DTOs + F-999 Item 28 + plan implementation 6 entités"
      - tool: OC
        id: "f3d9e1"
        nom: "hardening 7 entités (13 DTOs) + 14 nouveaux tests injection — 139/140 PASS"
      - tool: OC
        id: "20adf5"
        nom: "revue code + fix @IsNotEmpty TagDimension + fix test cleanup interfaces + analyse ZAP threat"
      - tool: OC
        id: "c7f3a1"
        nom: "commit résiduel T-101 — DTOs + tests validation (commit fad07bd)"
    notes: |
      Suite T-099 (data-objects validation hardening) : appliquer les mêmes contrôles
      à toutes les entités EA. Référence pattern : CreateDataObjectDto post-T-099.

      Audit préalable (2026-05-04) identifie ces gaps critiques/high :

      CRITIQUE (aucune garde sur champs libres) :
      - Interface : name, description, comment, technicalContact
      - Application : description, comment, lifecycleStatus

      HIGH (@Transform trim manquant, @MaxLength présent) :
      - ItComponent : description, comment, technology, type

      MEDIUM (@Matches manquant sur name, rest OK) :
      - Domain, Provider, BusinessCapability, Application, TagDimension

      Contrôles à appliquer sur ALL create + update DTOs :

      Name fields (tous) :
      + @Matches(/^[^;<>|`\\{}\[\]\x00-\x1F]*$/) — rejeter caractères injection
      + @NotContains('://') — rejeter URL schemes
      + @Transform trim si absent

      Description/Comment/Text fields (tous libres) :
      + @Transform(trim) si absent
      + @MaxLength(2000) si absent
      + Cohérence avec pattern data-objects

      Tests requis :
      - Chaque *.validation.api.spec.ts concerné : ajouter 1 test injection POST + PATCH
      - Validation 400 sur payloads: ZAP;*, ZAP|*, http://, ]]>, `backtick`
      - Sanity check: valid name avec espaces/tirets acceptée

      Lié à F-999 Item 28 (tech debt).
      Estimé 2-3 jours (6 DTOs × 2 files + tests + validation).

  - id: T-102
    nom: "Spec — FS-12 Dashboard : rédiger FS-12-dashboard-back.md + FS-12-dashboard-front.md"
    statut: done
    assigned_agent: spec
    session_active: ~
    handoff_ref: "docs/05-Project/20260506/SESSION-HANDOFF-spec-t102.md"
    date_resolution: 2026-05-06
    sprint: S5
    theme: "FS-12"
    type: spec
    feature: "FS-12"
    priorité: high
    sessions:
      - tool: CL
        id: "f2a8c1"
        nom: "spec FS-12-BACK + FS-12-FRONT rédigées (draft) — 2 fichiers, gates G-01..G-11, correction store/auth"
    notes: |
      Gate avant toute implémentation FS-12.
      Produire deux fichiers de spec opérationnelle :
      - FS-12-dashboard-back.md : HomeModule NestJS, GET /api/v1/home/summary (Promise.allSettled),
        migration Prisma user_domain_scope (D-01), amendment /auth/me domainIds[] (D-02),
        domain filtering ApplicationsService + BusinessCapabilitiesService (D-04, D-05).
      - FS-12-dashboard-front.md : home page 4 zones (layout V0), store/auth domainIds (correction :
        le projet utilise un store in-memory, pas React Context), US-HOME-09 user admin multi-select.
      Référence : docs/03-Features-Spec/FS-12-Dashboard/FS-12-dashboard-userstories.md
      (9 US, 7 dépendances techniques, décisions architecturales actées).

  - id: T-103
    nom: "Back — Implémenter FS-12 Dashboard backend (user_domain_scope + HomeModule BFF + amendments)"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: "docs/05-Project/20260506/SESSION-HANDOFF-back-t103.md"
    date_resolution: 2026-05-06
    sprint: S5
    theme: "FS-12"
    type: impl
    feature: "FS-12-BACK"
    priorité: high
    sessions:
      - tool: OC
        id: "c7f3a1"
        nom: "implémentation FS-12-BACK : UserDomainScope + HomeModule + amendments — G-01..G-11 validées"
    notes: |
      Scope complet backend FS-12 (dépend T-102) :
      D-01 : migration Prisma — modèle UserDomainScope (@@map user_domain_scope, N:N User↔Domain, pas de champ role en P1).
      D-02 : amendment AuthService.getProfile() — inclure domainIds[] + noms des domaines assignés.
      D-03 : nouveau HomeModule — GET /api/v1/home/summary, orchestre 6 appels internes via
             Promise.allSettled(), réponse 200 toujours (champs null si erreur partielle).
      D-04 : amendment ApplicationsService — count() + findAll() filtré sur domainIds[] du user.
      D-05 : amendment BusinessCapabilitiesService — count() filtré sur domainIds[].
      Convention scope : 0 domaine assigné = portée globale (pas de flag is_global_scope).

  - id: T-104
    nom: "QA-back — Tests API FS-12 Dashboard (HomeModule + scope domaine)"
    statut: done
    assigned_agent: qa
    session_active: ~
    handoff_ref: "docs/05-Project/20260506/SESSION-HANDOFF-qa-t104.md"
    date_resolution: 2026-05-06
    sprint: S5
    theme: "FS-12"
    type: test
    feature: "FS-12-BACK"
    priorité: medium
    sessions:
      - tool: OC
        id: "0e5d84"
        nom: "tests API FS-12 home/summary + scope domaine + RBAC 403"
    notes: |
      Dépend T-103.
      Scope : e2e/tests/home/home-summary.api.spec.ts
      - GET /api/v1/home/summary → 200 avec les 4 zones peuplées
      - Scope domaine : user avec domaines assignés vs user global (0 domaine)
      - Erreur partielle : champ null si sous-service échoue, reste 200
      - RBAC : 401 sans token, 403 si permission manquante (applications:read)

  - id: T-105
    nom: "Front — Implémenter FS-12 Dashboard (home page 4 zones + US-HOME-09 user admin)"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: "docs/05-Project/20260506/SESSION-HANDOFF-front-t105.md"
    date_resolution: 2026-05-06
    sprint: S5
    theme: "FS-12"
    type: impl
    feature: "FS-12-FRONT"
    priorité: high
    sessions:
      - tool: OC
        id: "f5a3d1"
        nom: "implémentation FS-12-FRONT dashboard + US-HOME-09 + corrections review"
    notes: |
      Dépend T-103.
      Scope US-HOME-01..08 : page d'accueil route `/`
      - Zone 1 : 4 tuiles KPI (Bandeau, cliquables → navigate)
      - Zone 2 : fiches incomplètes (5 max) + contrats expirant 90j (5 max)
      - Zone 3 : bar chart lifecycle via LinearProgress MUI (pas de lib graphique externe)
      - Zone 4 : gauge data quality (LinearProgress, couleur dynamique error/warning/success)
      - Zone transverse : bienvenue contextualisé (1/N/global domaine), EmptyState si 0 app
      Layout V0 : Zone1 full width → Zone2 full width → Zones3+4 côte à côte 2/3+1/3, maxWidth xl.
      US-HOME-09 : champ multi-select domaines dans page création/édition utilisateur (admin).
      AuthContext : étendre le store auth avec domainIds[] (issus de /auth/me).

  - id: T-106
    nom: "QA-front — Tests e2e FS-12 Dashboard (parcours Playwright)"
    statut: done
    assigned_agent: qa
    session_active: ~
    handoff_ref: "docs/05-Project/20260506/SESSION-HANDOFF-qa-t106.md"
    date_resolution: 2026-05-06
    sprint: S5
    theme: "FS-12"
    type: test
    feature: "FS-12-FRONT"
    priorité: medium
    sessions:
      - tool: OC
        id: "b701a2"
        nom: "tests e2e dashboard FS-12 UI (13 cas)"
    notes: |
      Dépend T-105.
      Parcours à couvrir (Playwright UI) :
      - Marc (AE, 1 domaine) : tuiles KPI visibles, navigation depuis tuile, fiches incomplètes listées
      - Thierry (CIO, 0 domaine) : portée globale, bandeau "Tous les domaines"
      - User lecture seule (pas applications:read) : section "À traiter" masquée
      - EmptyState : périmètre sans application → zones 2/3/4 remplacées par EmptyState

  - id: T-107
    nom: "QA — Review findings Cypress it-components (emplacement, noms fixes, assertion seed)"
    statut: open
    assigned_agent: qa
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: S5
    theme: "QA"
    type: review
    feature: "QA-CYPRESS"
    priorité: low
    sessions:
      - tool: OC
        id: "0e5d84"
        nom: "création tâche review findings Cypress it-components"
    notes: |
      3 findings remontés par code review (session T-104, 2026-05-06) sur
      e2e/tests/04-it-components.cy.ts :

      1. [Medium] Fichier Cypress dans e2e/tests/ au lieu de frontend/cypress/e2e/.
         Risque : tests silencieusement exclus des runs CI/locaux.
         Action : vérifier si le fichier est déclenché quelque part, déplacer si nécessaire.

      2. [Medium] Noms de données de test fixes (ex: "Test Component Cypress", "ToDelete Cypress Component").
         Risque : contrainte de doublon lors de reruns si cleanup échoue.
         Action : préfixer avec Date.now() ou timestamp unique.

      3. [Low] Assertion dépendante de l'ordre/seed ("first row has linked apps").
         Risque : test fragile si tri ou seed change.
         Action : créer les données de test explicitement plutôt que d'hypothéquer la seed.

  - id: T-108
    nom: "Playwright — l'intégralité des tests UI plante contrairement aux tests backend"
    statut: done
    assigned_agent: qa
    session_active: ~
    handoff_ref: "docs/05-Project/20260506/SESSION-HANDOFF-qa-t108.md"
    date_resolution: 2026-05-06
    sprint: S5
    theme: "QA"
    type: bug
    feature: "QA-E2E-UI"
    priorité: high
    sessions:
      - tool: OC
        id: "6f4b2a"
        nom: "stabilisation UI Playwright + provisionnement readonly"
    notes: |
      Résolution complète de la panne UI Playwright.
      Actions principales :
      - Makefile `test-e2e` local-first + fallback Docker
      - `e2e/playwright.config.ts` baseURL local + `globalSetup`
      - `e2e/global-setup.ts` : provisionnement idempotent rôle/user `ReadOnly`
      - suppression des `test.skip(!readonlyOk, ...)` sur les specs UI read-only
      - stabilisation des helpers login (`/auth/me`) et sélecteurs fragiles
      Validation : exécution UI verte et compte readonly opérationnel.

  - id: T-109
    nom: "MegaLinter — corriger plugin sonarjs ESLint v8 + calibrer jscpd sur codebase réelle"
    statut: done
    assigned_agent: arch
    session_active: ~
    handoff_ref: ~
    date_resolution: 2026-05-06
    sprint: S5
    theme: "QA"
    type: debt
    feature: "QA-MEGALINTER"
    priorité: medium
    sessions:
      - tool: OC
        id: "a7c91f"
        nom: "correction sonarjs + calibration jscpd"
    notes: |
      Cause sonarjs : eslint-plugin-sonarjs v4.0.2 incompatible avec le format legacy ESLint v8 de MegaLinter.
      Fix : PRE_COMMANDS dans .mega-linter.yml installe sonarjs@1.0.4 dans /node-deps/ au runtime
      (dernière version supportant ESLint v8 legacy config + peerDep ^8||^9).

      jscpd recalibré pour ne mesurer que la codebase réelle :
      - Exclusions .agents/**, .claude/**, .opencode/**
      - languages: ["typescript", "javascript"]
      - threshold: 10 → 8

      Résultat : taux de duplication mesuré sur vraie codebase à 8.35%, HTML copy-paste restauré.
      Fichiers modifiés : .mega-linter.yml, backend/.eslintrc.cjs, .jscpd.json.

  - id: T-110
    nom: "Arch — Ajouter vice (Webba) dans les scans (Makefile + reports + dashboard)"
    statut: open
    assigned_agent: arch
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: S5
    theme: "QA"
    type: impl
    feature: "QA-SCANS"
    priorité: medium
    sessions: []
    notes: |
      Intégrer https://github.com/Webba-Creative-Technologies/vice dans le pipeline de scans ARK-EPM.
      Contrainte : impacts minimum sur Makefile, reports/, et dashboard reports.
      Scope attendu :
      - Ajouter une target Makefile (ex: scan-vice) sans casser les targets existantes
      - Configurer la sortie des rapports dans reports/ avec horodatage (cohérence T-069)
      - Vérifier compatibilité avec le dashboard reports existant

  - id: T-111
    nom: "Finaliser et tester l'analyse LLM phase 2 du reports dashboard (intégration Anthropic + Makefile)"
    statut: open
    assigned_agent: arch
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: S5
    theme: "QA"
    type: impl
    feature: "QA-REPORTS"
    priorité: medium
    sessions: []
    notes: |
      Suite de e68fa60 : tester l'analyse LLM avec clé Anthropic réelle,
      committer le Makefile (targets reports-manifest, reports-dashboard,
      hooks scan->manifest, JSON Trivy compagnon), valider le rendu
      sur http://localhost:4002/docs/05-Project/reports-dashboard/.

  - id: T-112
    nom: "Front — Dashboard : layout 2 lignes sous KPIs + tooltips sur chaque bloc"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: "docs/05-Project/20260506/SESSION-HANDOFF-front-t112.md"
    date_resolution: 2026-05-06
    sprint: S5
    theme: "FS-12"
    type: impl
    feature: "FS-12-FRONT"
    priorité: low
    sessions:
      - tool: OC
        id: "9f2b6c"
        nom: "dashboard home layout+tooltips"
    notes: |
      Deux améliorations subtiles du dashboard d'accueil (post-T-105).

      1. Layout — repositionner les blocs sous les KPIs :
         Actuellement Zone 3 + Zone 4 côte à côte (2/3 + 1/3).
         Cible : 2 lignes distinctes sous la bande KPIs (Zone 1),
         pour plus de lisibilité et d'espace par bloc.

      2. Tooltips — étendre à tous les blocs :
         "Qualité des données" possède déjà un tooltip définissant sa métrique.
         Ajouter un tooltip équivalent sur chacun des autres blocs
         (KPIs, fiches incomplètes/contrats, distribution cycle de vie).
         Chaque tooltip doit expliquer ce que le bloc mesure / comment il se calcule.

      Fichier cible : frontend/src/pages/home/HomePage.tsx

  - id: T-113
    nom: "QA — Dashboard coverage : aligner comptage Playwright et inventaire fichiers"
    statut: done
    assigned_agent: qa
    session_active: ~
    handoff_ref: "docs/05-Project/20260506/SESSION-HANDOFF-qa-t108.md"
    date_resolution: 2026-05-06
    sprint: S5
    theme: "QA"
    type: bug
    feature: "QA-REPORTS"
    priorité: medium
    sessions:
      - tool: OC
        id: "6f4b2a"
        nom: "fix aggregate-coverage Playwright path mapping"
    notes: |
      Correction `scripts/aggregate-coverage.js` dans `resolveProjectPath`.
      Problème : `spec.file` Playwright est relatif à `e2e/tests` (ex: `applications/...`),
      ce qui empêchait le matching avec l'inventaire (`e2e/tests/...`) et masquait les stats par fichier.
      Fix : normalisation explicite des chemins Playwright vers `e2e/tests/${cleaned}`.
      Puis régénération de `docs/05-Project/test-coverage-dashboard/coverage-data.json`.

  - id: T-114
    nom: "FS-12 — Dashboard polish : BC context dans fiches incomplètes + ancrage tooltip KPI"
    statut: done
    assigned_agent: front
    session_active: ~
    handoff_ref: "docs/05-Project/20260506/SESSION-HANDOFF-front-fs12-dashboard-polish.md"
    date_resolution: 2026-05-06
    sprint: S5
    theme: "FS-12"
    type: impl
    feature: "FS-12-FRONT"
    priorité: medium
    sessions:
      - tool: OC
        id: "e6b4f2"
        nom: "évolution widget fiches incomplètes + fix ancrage tooltip KPI"
    notes: |
      Ajustements post-T-105/T-112 du dashboard Home.
      Réalisé :
      - Widget "Fiches incomplètes" enrichi avec contexte Business Capability
        (`businessCapability` + `ancestors[]`) et fallback i18n si non renseigné.
      - Règle N:N app↔BC déterministe : profondeur max, tie-break `name ASC`, puis `id ASC`.
      - Rendu UI gauche→droite (nom + breadcrumb BC à gauche, chips manquants pointillés neutres à droite).
      - KPI band : icône tooltip "i" ancrée à droite dans chaque tuile.

      Impacts transverses appliqués et alignés :
      - Backend (DTO/service/tests), frontend (types/composants/i18n), OpenAPI et specs FS-12.
      - Build frontend validé.
      - Validation backend locale à rejouer dans un env disposant de `jest`/`nest`.

  - id: T-115
    nom: "Spec — Espace Utilisateur : Settings + i18n EN (FS-13)"
    statut: done
    assigned_agent: spec
    session_active: ~
    handoff_ref: "docs/05-Project/20260518/SESSION-HANDOFF-spec-t115.md"
    date_resolution: 2026-05-18
    sprint: S5
    theme: "FS-13"
    type: spec
    feature: "FS-13-BACK"
    priorité: medium
    sessions:
      - tool: OC
        id: "b749a5"
        nom: "rédaction FS-13-userstories.md + FS-13-BACK.md + FS-13-FRONT.md — interview 13 questions, 7 décisions structurantes verrouillées"
    notes: |
      Périmètre : Settings utilisateur + traduction anglaise complète.
      Livrables :
      - FS-13-userstories.md (US en préalable)
      - FS-13-BACK.md (endpoint settings, choix langue, structure user preferences)
      - FS-13-FRONT.md (page settings, toggle FR/EN, thème)
      Dépendances : F-02-i18n existant, F-99-Technical-Debt (items i18n).

  - id: T-116
    nom: "Back — Implémenter FS-13 Espace Utilisateur (user_preferences + PATCH /users/me + amendments auth)"
    statut: done
    assigned_agent: back
    session_active: ~
    handoff_ref: ~
    date_resolution: "2026-07-14"
    sprint: S5
    theme: "FS-13"
    type: impl
    feature: "FS-13-BACK"
    priorité: medium
    sessions:
      - tool: OC
        id: "40a9b5"
        nom: "Implémentation FS-13-BACK + corrections refresh/openapi + commit"
    notes: |
      Dépend de T-115 (spec done).
      Spec : docs/03-Features-Spec/FS-13-User-Settings/FS-13-BACK.md
      Livré (commit) :
        D-01 : migration Prisma UserPreference (1:1 users, language VarChar(10), default "fr")
        D-02 : amendment AuthService.getProfile() + login/refresh → preferences.language
        D-03 : nouveau endpoint PATCH /api/v1/users/me (UpdateMeDto, JWT uniquement)
        D-04 : amendment UsersService.create() → auto-create UserPreference au signup
        D-05 : migration data legacy → backfill user_preferences pour users existants
        D-06 : correction auth.controller.ts refresh pour inclure preferences
        D-07 : mise à jour docs/04-Tech/openapi.yaml (UserResponse.preferences + PATCH /users/me)
      Tests reportés à T-116-suite / T-118 : Jest unit (services) + Playwright API (endpoints).
      FS-13-BACK statut `done`. Bloque T-117 (front) — débloquée.

  - id: T-117
    nom: "Front — Implémenter FS-13 Espace Utilisateur (SettingsPage + avatar dropdown + i18n dynamique)"
    statut: open
    assigned_agent: front
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: S5
    theme: "FS-13"
    type: impl
    feature: "FS-13-FRONT"
    priorité: medium
    sessions: []
    notes: |
      Prête à démarrer : T-116 done (2026-07-14). Gates G-01..G-10 validées (tests reportés à T-116-suite).
      Spec : docs/03-Features-Spec/FS-13-User-Settings/FS-13-FRONT.md
      Scope :
        - SettingsPage (/settings) avec Select langue FR/EN, sauvegarde immédiate
        - Avatar + dropdown dans TopBar (nom + Paramètres + Déconnexion)
        - Suppression footer Sidebar (nom user + bouton logout)
        - Amendment store/auth.ts → getLanguage() helper
        - Amendment types/auth.ts → preferences dans UserResponse
        - Amendment i18n/index.ts → support dynamique changeLanguage()
        - Création en.json minimal (settings + common + nav)
        - Amendment App.tsx → chargement langue au boot via /auth/me

  - id: T-118
    nom: "Arch — Décision : remplacer le gate 'FS-XX-BACK done avant FS-XX-FRONT stable' par un gate 'contrat API stable'"
    statut: open
    assigned_agent: arch
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: ~
    theme: "GOVERNANCE"
    type: decision
    feature: "GOVERNANCE-GATE"
    priorité: high
    sessions: []
    notes: |
      Issu de l'audit croisé de gouvernance (3 revues IA, 2026-07-14, docs/05-Project/20260714/).
      Constat : docs/AGENTS.md §2 interdit à la spec front de passer `stable` tant que la spec
      back n'est pas `done`, ce qui force une chaîne linéaire spec back → impl back → spec front
      → impl front → QA, alors que le front n'a souvent besoin que du contrat (chemins, DTOs,
      erreurs, permissions, pagination) pour démarrer layout/i18n/types/React Query/parcours.
      Proposition à valider : contrat API stable (OpenAPI + DTO/erreurs/permissions) → spec front
      stable → impl back/front en parallèle → gate d'intégration unique (API réelle + tests API +
      1 parcours E2E critique). Le backend reste requis avant l'intégration, pas avant toute
      conception/implémentation front. Mock temporaire limité au contrat toléré en attendant l'API.
      Si validé : mettre à jour docs/AGENTS.md §2 (Séquencement) et le Cycle de feature associé.

  - id: T-119
    nom: "Arch — Décision : redistribuer le RACI tests entre implémenteurs (back/front/data) et QA"
    statut: open
    assigned_agent: arch
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: ~
    theme: "GOVERNANCE"
    type: decision
    feature: "GOVERNANCE-RACI"
    priorité: high
    sessions: []
    notes: |
      Issu de l'audit croisé de gouvernance (2026-07-14).
      Constat : QA est R sur "Tests (toutes couches)" dans le RACI racine, mais ne peut pas
      modifier le code source (e2e/AGENTS.md). La règle "bug = test de non-régression avant
      correction" impose alors 3 handoffs (QA reproduit → back/front corrige → QA revalide)
      même pour un bug trivial avec test ciblé.
      Proposition à valider : back/front/data écrivent et maintiennent les tests directement liés
      à leur changement (unitaire, API, ou UI ciblé) — une même session peut ajouter un test rouge,
      corriger, vérifier vert. QA reste R sur la stratégie de test, les fixtures communes, les
      parcours E2E critiques transverses, la stabilité globale et la revue de couverture. QA n'est
      sollicité que si le test est transverse, instable, ou nécessite une stratégie nouvelle.
      Si validé : mettre à jour le RACI racine (AGENTS.md §3) et e2e/AGENTS.md en conséquence.

  - id: T-120
    nom: "Arch — Décision : mécanisme de verrouillage non atomique de tasks.yaml (risque de collision multi-session)"
    statut: open
    assigned_agent: arch
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: ~
    theme: "GOVERNANCE"
    type: decision
    feature: "GOVERNANCE-LOCK"
    priorité: high
    sessions: []
    notes: |
      Issu de l'audit croisé de gouvernance (2026-07-14).
      Constat : le rituel /ark-open-session lit puis écrit tasks.yaml sans verrou atomique — deux
      agents peuvent lire la même tâche `open` et la verrouiller simultanément, ou calculer le même
      prochain ID (max(id)+1) en parallèle. Le fichier est un registre partagé en clair, sans
      validation de schéma ni transaction.
      Voir aussi T-003 : verrou `session_active` orphelin détecté (session_active ≠ id dans
      sessions[]), symptôme concret du problème.
      Pistes à évaluer (non tranchées) : relecture + interruption si le verrou a changé avant
      d'écrire, script de mutation unique validant le schéma et choisissant max(id)+1, ou un
      registre transactionnel (issue GitHub/GitLab) si la concurrence devient fréquente.
      Ne pas résoudre par convention textuelle seule.

  - id: T-121
    nom: "Arch — Décision : assouplir la validation arch systématique des migrations Prisma locales"
    statut: open
    assigned_agent: arch
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: ~
    theme: "GOVERNANCE"
    type: decision
    feature: "GOVERNANCE-PRISMA-GATE"
    priorité: medium
    sessions: []
    notes: |
      Issu de l'audit croisé de gouvernance (2026-07-14).
      Constat : AGENTS.md §1 exige la validation arch avant toute modification de schema.prisma,
      y compris pour un ajout local conforme à un pattern déjà validé (ex : nouveau champ optionnel
      sur une entité existante). Goulot d'étranglement pour data.
      Proposition à valider : data implémente sans gate les migrations locales conformes au schéma
      existant ; arch valide uniquement les changements de contrat, modèle transverse, rupture de
      compatibilité, cardinalité, index volumineux, trigger, ou sécurité. Décision enregistrée en
      ADR courte si structurante (voir T-123).

  - id: T-122
    nom: "Arch — Décision : conditions d'autorisation de suppression de code sans confirmation préalable"
    statut: open
    assigned_agent: arch
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: ~
    theme: "GOVERNANCE"
    type: decision
    feature: "GOVERNANCE-CODE-DELETE"
    priorité: medium
    sessions: []
    notes: |
      Issu de l'audit croisé de gouvernance (2026-07-14).
      Constat : AGENTS.md §1 Règle 3 impose de "proposer la suppression, ne pas l'exécuter sans
      confirmation" pour tout code existant — cela bloque les refactors, le retrait de code mort et
      les corrections de sécurité, et est déjà contredit en pratique par des tâches qui demandent
      explicitement des suppressions après migration (ex : T-009 retrait Cypress après migration
      Playwright).
      Proposition à valider : autoriser la suppression sans confirmation quand le code est introduit
      dans la même session, non référencé ailleurs, remplacé dans le même changement, ou couvert par
      un test ciblant le comportement conservé. Confirmation requise seulement pour une API publique,
      une migration de données irréversible, un comportement utilisateur incertain, ou une
      suppression à large périmètre.

  - id: T-123
    nom: "Arch — Décision : remplacer les commentaires AGENT-DECISION dans le code par des ADR courtes"
    statut: open
    assigned_agent: arch
    session_active: ~
    handoff_ref: ~
    date_resolution: ~
    sprint: ~
    theme: "GOVERNANCE"
    type: decision
    feature: "GOVERNANCE-ADR"
    priorité: low
    sessions: []
    notes: |
      Issu de l'audit croisé de gouvernance (2026-07-14).
      Constat : AGENTS.md §1 Règle 2 impose un commentaire `// AGENT-DECISION: [agent] — raison`
      pour toute décision structurante dans le code. Ces commentaires deviennent obsolètes et se
      dupliquent au fil des changements.
      Proposition à valider : créer `docs/04-Tech/decisions/ADR-NNN-<slug>.md` (court, daté) pour
      toute décision structurante ; la tâche/spec/code référence l'ADR par son identifiant. Réserver
      les commentaires de code aux invariants locaux difficiles à déduire du contexte.
      Si validé : mettre à jour AGENTS.md §1 Règle 2 et créer docs/04-Tech/decisions/.

  - id: T-124
    nom: "Arch — Corrections de cohérence gouvernance suite à l'audit croisé (3 revues IA)"
    statut: done
    assigned_agent: arch
    session_active: ~
    handoff_ref: "docs/05-Project/20260714/SESSION-HANDOFF-arch-croisement-gouvernance.md"
    date_resolution: "2026-07-14"
    sprint: S5
    theme: "GOVERNANCE"
    type: debt
    feature: "GOVERNANCE-CONSISTENCY"
    priorité: high
    sessions: []
    notes: |
      Suite au croisement des 3 audits docs/05-Project/20260714/global-review-*.md.
      Corrections appliquées (voir handoff_ref pour le détail complet) :
      - MUI v5→v9 dans ark-front.md et opencode.json
      - Ajout agents/commandes ark-arch et ark-spec (manquants)
      - Chemin DESIGN.md corrigé (frontend/AGENTS.md, docs/AGENTS.md)
      - docs/AGENTS.md §1 arborescence dupliquée nettoyée, §3 table specs rafraîchie
      - Schéma YAML tasks.yaml corrigé dans AGENTS.md et ark-close-session.md
      - SESSION-HANDOFF.md racine entièrement rafraîchi contre tasks.yaml réel
      Décisions structurelles non tranchées déportées vers T-118 à T-123.

  - id: T-125
    nom: "Arch — Mettre en place l'orchestration supervisée ARK pour OpenCode et Claude Code"
    statut: done
    assigned_agent: arch
    session_active: ~
    handoff_ref: "docs/05-Project/20260714/SESSION-HANDOFF-arch-orchestration-supervisee.md"
    date_resolution: "2026-07-14"
    sprint: S5
    theme: "GOVERNANCE"
    type: impl
    feature: "GOVERNANCE-ORCHESTRATION"
    priorité: high
    sessions: []
    notes: |
      Décision utilisateur du 2026-07-14 : mode supervisé et compatibilité OpenCode + Claude Code.
      Périmètre : agent primaire non-mutant, sous-agents métier sans délégation récursive,
      approbation avant délégation mutante, politique commune et ADR.
      Livré : ADR-001, politique docs/04-Tech/agent-orchestration.md, 7 définitions Claude
      (.claude/agents/) et configuration OpenCode. Validé : JSON/YAML, `claude -p --agent
      ark-orchestrator` et `opencode run --agent ark-orchestrator` répondent sans outil.
